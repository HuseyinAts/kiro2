# FastAPI ve temel bağımlılıklar
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Veritabanı bağımlılıkları
asyncpg==0.29.0  # PostgreSQL async driver
psycopg2-binary==2.9.9  # PostgreSQL sync driver (Alembic için)
sqlalchemy[asyncio]==2.0.23
alembic==1.13.1

# Redis cache
redis[hiredis]==5.0.1
aioredis==2.0.1

# Celery - Background Task Processing (Sprint 3)
celery[redis]==5.3.4
flower==2.0.1  # Celery monitoring dashboard
kombu==5.3.4  # Celery messaging library

# Test bağımlılıkları
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-xdist==3.8.0

# Elasticsearch
elasticsearch[async]==8.11.0

# HTTP client
aiohttp==3.9.1
httpx==0.25.2

# Güvenlik
passlib[bcrypt]==1.7.4
slowapi==0.1.9  # DDoS protection and rate limiting
bleach==6.2.0  # XSS protection

# Sprint 4: 2FA (Two-Factor Authentication)
pyotp==2.9.0  # TOTP (Time-based One-Time Password)
qrcode[pil]==7.4.2  # QR code generation
Pillow==10.1.0  # Image processing for QR codes

# Türkçe NLP
zemberek-python==0.1.3
hijri-converter==2.3.1
langdetect==1.0.9  # Dil tespiti için

# Monitoring ve logging
prometheus-client==0.19.0
psutil==5.9.6
aiofiles==23.2.1
structlog==24.1.0  # Structured logging

# Analytics ve ML
numpy==1.24.4
scikit-learn==1.3.2

# Matematiksel ve Görsel İşlemler (Task 55)
sympy==1.12  # Matematiksel doğrulama
matplotlib==3.8.2  # Grafik ve görsel üretimi
plotly==5.18.0  # İnteraktif grafikler

# NLP Training Pipeline (Task 54)
openai==1.3.0  # GPT-4 fine-tuning
transformers==4.35.0  # BERTurk, T5, BART
torch==2.1.0  # PyTorch
nltk==3.8.1  # BLEU score
rouge-score==0.1.2  # ROUGE score
sentencepiece==0.1.99  # Tokenization

# BERTurk ve Transformers
torch>=2.0.0
transformers>=4.30.0

# PDF oluşturma ve Excel export
reportlab==4.0.7
matplotlib==3.7.4
xlsxwriter==3.1.9

# JWT token işleme
PyJWT==2.8.0

# YouTube ve Web Scraping
beautifulsoup4==4.12.2

# Geliştirme araçları (moved to dev-requirements if needed)
# Text-to-Speech (Task 79)
gTTS==2.5.0  # Google Text-to-Speech (online, high quality)
pyttsx3==2.90  # Offline TTS (fallback)

# PDF Parsing and OCR (ÖSYM Question Extraction)
pdfplumber==0.11.0  # PDF extraction
PyPDF2==3.0.1  # PDF manipulation
pytesseract==0.3.10  # OCR engine
Pillow==10.1.0  # Image processing
pdf2image==1.16.3  # PDF to image conversion

# Sprint 11: Distributed Tracing (OpenTelemetry + Jaeger)
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-instrumentation==0.42b0
opentelemetry-instrumentation-fastapi==0.42b0
opentelemetry-instrumentation-sqlalchemy==0.42b0
opentelemetry-instrumentation-redis==0.42b0
opentelemetry-instrumentation-requests==0.42b0
opentelemetry-instrumentation-logging==0.42b0
opentelemetry-exporter-jaeger==1.21.0
opentelemetry-exporter-jaeger-thrift==1.21.0
opentelemetry-exporter-otlp==1.21.0

# Sprint 12: Error Tracking (Sentry)
sentry-sdk[fastapi]==1.40.0
