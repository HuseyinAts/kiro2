# Minimal Requirements for MVP Beta Launch
# Use this for the backend Docker image

# Core Dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic[email]
python-multipart>=0.0.6

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Auth & Security
PyJWT>=2.8.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<5

# Cache & Redis
redis>=5.0.0
cachetools>=5.0.0  # TTLCache — required by core.osym_exam_engine + agents.learning_path (GF3/GF7)

# Async & HTTP
aiohttp>=3.9.1
httpx>=0.25.2

# Logging
structlog>=23.1.0

# Metrics (needed by learning_path, youtube_routes via core/metrics_collector)
prometheus_client>=0.19.0

# Basic utilities
numpy>=1.24.3
psutil>=5.9.0

# Monitoring & Metrics (prometheus_client also listed above â€” duplicate removed)
# ============================================================
# EKSIK PAKETLER â€” docker log'dan tespit (22 Mart 2026)
# ============================================================

# YouTube routes â€” YOUTUBE_API_KEY aktif olur
slowapi>=0.1.9

# Analytics export
xlsxwriter>=3.1.0

# Gelismis raporlar / IRT hesaplari
scipy>=1.11.0

# Sifreleme API'si
cryptography>=41.0.0

# Celery gorevleri
celery[redis]>=5.3.0

# 2FA
pyotp>=2.9.0

# Video cozum yukleme
aiofiles>=23.2.0

# Sentry (production hata izleme)
sentry-sdk[fastapi]>=1.40.0

# YouTube HTML scraping
beautifulsoup4>=4.12.0

# Hibrit soru uretimi
anthropic>=0.40.0

# PDF isleme
pdfplumber>=0.10.0
pypdf2>=3.0.0

# Veri analizi
pandas>=2.0.0

# 2. tur eksik paketler (22 Mart 2026)
qrcode[pil]>=7.4.0
openai>=1.50.0
hijri-converter>=2.3.1
matplotlib>=3.7.0
reportlab>=4.0.0
networkx>=3.1

# Social content moderation (emoji abuse detection)
emoji>=2.8.0

# P0 paketler â€” plan FAZ 2 (31 Mart 2026)
# TÃ¼rkÃ§e NLP (chat, soru analizi)
zemberek-python>=0.1.2
# OAuth2 sosyal giriÅŸ (Google, Apple)
authlib>=1.3.0
# GÃ¶rÃ¼ntÃ¼ iÅŸleme (qrcode[pil] baÄŸÄ±mlÄ±lÄ±ÄŸÄ±, 2FA QR)
Pillow>=10.0.0
# Semantik arama (pgvector yanÄ±nda Elasticsearch desteÄŸi)
elasticsearch[async]==8.11.0

# ChromaDB — semantic_search / content_recommendation / clustering / duplicate_detection
chromadb>=0.4.22,<0.6


# Text-to-Speech
gtts>=2.4.0
pyttsx3>=2.90

# JWT (python-jose)
python-jose[cryptography]>=3.3.0
uuid6>=2024.1.1

pgvector>=0.2.1
