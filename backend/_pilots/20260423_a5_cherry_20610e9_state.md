# Pilot: A5 Cherry-Pick — `20610e9` Kısmi (Borç #6)

**Tarih:** 2026-04-23  
**Araç:** Cursor Agent mode (terminal)  
**Bağlı borç:** #6 — `40_OPEN_DEBTS` §Borç #6 Kalan İş 1. madde (Round 2 plan §A5)  
**Kaynak plan:** `.cursor/plans/20260422_autopilot_debt_6_review.md` §A5  

**Durum:** ADIM 0 tamam — **ADIM 1 bekliyor (state.md onayı)**

---

## ADIM 0 — Envanter ve kapsam

### Kaynak commit

- **SHA:** `20610e92a784b759169d0a56defd7b42f3925872`
- **Konum:** `master` içinde **yok** (`merge-base --is-ancestor` → hayır); yalnızca `autopilot/student-ready-20260421` (ve `origin`) zincirinde.
- **Mesaj özeti:** admin analytics auth shim, generator import path, chroma health routes.

### Dosya bazında diff özeti (`git show 20610e9 --stat`)

| Dosya | Satır | Bu pilot (kullanıcı YASAK/kapsam) |
|--------|------|-----------------------------------|
| `backend/core/automated_question_generator.py` | +5 (yeni dosya) | **ALINACAK** — public re-export; `_deprecated` impl. |
| `backend/tests/fast/test_api_coverage_batch9.py` | +32 | **ALINMAYACAK** — “PLAN DIŞI TEST EKLEME YASAK”; pilot sadece import path. |
| `backend/tests/fast/test_api_coverage_batch13.py` | +32 | **ALINMAYACAK** — aynı. |
| `backend/tests/fast/test_chroma_semantic_health.py` | +26 | **ALINMAYACAK** — Chroma sağlık rotası / kapsam dışı (plan §A5). |

### Master ile uyum (kanıt)

- `backend/core/automated_question_generator.py` **yok**; yalnızca `backend/core/_deprecated/automated_question_generator.py` var.
- Birden fazla test `from core.automated_question_generator import AutomatedQuestionGenerator` kullanıyor (`test_question_generator_methods.py`, `test_automated_question_generator.py`, `integration/...`, `slow/...`). Re-export dosyası bu import’u düzeltir.

### Risk notu (bilinçli)

- `20610e9` içindeki **batch9/batch13** değişiklikleri analytics admin route’ları için `authenticate_user` + RBAC mock ekler. Bu pilot bunları **almıyor**. İlgili fast testlerde auth/RBAC kaynaklı kırılma **olası**; bu pilot kapsamı dışında bırakıldı (kullanıcı talimatı).

---

## YASAK özeti (bu oturum)

- `git add -A` — yasak; yalnızca seçici `git add`.
- `git cherry-pick` ile doğrudan tam commit (özellikle `--no-ff` tam merge) — yerine **`-n` / `--no-commit`** + el ile stage; veya tek dosyayı `git show` ile çıkarma.
- Chroma sağlık testleri / rotaları bu commit’e **girmeyecek**.
- `alembic upgrade` yok.
- `master` dışı branch’e checkout yok.
- Yeni unit test dosyası / plan dışı test genişletmesi yok.

---

## ADIM 1 — Önerilen komutlar (**onay sonrası**)

Aşağıdakiler yalnızca `state.md` onaylandıktan sonra çalıştırılır.

### Seçenek A — `cherry-pick -n` + test dosyalarını geri al

```powershell
cd C:\Users\husey\kiro2
git cherry-pick -n 20610e9
# Sadece core dosyası kalsın; test ve chroma değişikliklerini at
git restore --staged --worktree backend/tests/fast/test_api_coverage_batch9.py `
  backend/tests/fast/test_api_coverage_batch13.py `
  backend/tests/fast/test_chroma_semantic_health.py
git add backend/core/automated_question_generator.py
git diff --cached --stat
# Sonra ayrı commit mesajı (kullanıcı / pilot protokolü)
```

### Seçenek B — Tek dosyayı commit’ten çıkarmak (cherry-pick yok)

```powershell
cd C:\Users\husey\kiro2
git show 20610e9:backend/core/automated_question_generator.py | Out-File -Encoding utf8NoBOM backend\core\automated_question_generator.py
# İçeriği 20610e9 ile birebir karşılaştır: git diff backend/core/automated_question_generator.py
git add backend/core/automated_question_generator.py
```

*(PowerShell `Out-File` BOM/satır sonu farkı yaratabilir; tercih: Seçenek A veya editörle yapıştırma.)*

---

## Onay kutusu

- [ ] Hüseyin: ADIM 1 için **Seçenek A** veya **B** onayı  
- [ ] Commit mesajı / `--no-verify` ihtiyacı netleştirildi mi?  

**Onay metni örneği:** “ADIM 1 Seçenek A, commit at” veya “Sadece dosyayı ekle, commit benim”.

---

## ADIM 2 (sonra)

- `git log -1`, `git status --short` teyidi.  
- Gerekirse `40_OPEN_DEBTS` §Borç #6 “20610e9 kısmi” maddesini kapat / güncelle (ayrı görev).
