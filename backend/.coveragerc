[run]
# Specific source directories for better performance
source = api,services,core,algorithms,analytics,agents
branch = true

# Comprehensive omit list
omit =
    */tests/*
    */test_*
    *_test.py
    */venv/*
    */env/*
    */.venv/*
    */__pycache__/*
    */migrations/*
    */alembic/*
    */static/*
    */media/*
    */uploads/*
    */htmlcov/*
    */scripts/*
    */site-packages/*
    */node_modules/*
    */frontend/*
    */cache/*
    */config/*
    setup.py
    manage.py
    conftest.py
    run_*.py
    test_*.py
    *_demo.py
    demo_*.py
    verify_*.py
    install_*.py
    quick_*.py
    ultra_*.py
    minimal_*.py
    simple_*.py
    mock_*.py
    analyze_*.py
    count_*.py
    _*.py

[report]
# Precision for coverage percentages
precision = 2

# Show missing lines
show_missing = true

# Skip empty files
skip_empty = true

# Skip covered files for cleaner output
skip_covered = true

# Sort by coverage percentage
sort = cover

# Exclude lines from coverage
exclude_lines =
    # Standard pragma
    pragma: no cover

    # Don't complain about missing debug code
    def __repr__
    def __str__

    # Don't complain if settings.DEBUG
    if self.debug:
    if settings.DEBUG

    # Don't complain if tests don't hit defensive assertion code
    raise AssertionError
    raise NotImplementedError

    # Don't complain if non-runnable code isn't run
    if 0:
    if __name__ == .__main__.:
    if TYPE_CHECKING:
    if typing.TYPE_CHECKING:

    # Don't complain about protocol methods
    class .*\bProtocol\):

    # Don't complain about abstract methods
    @abstractmethod
    @abc.abstractmethod
    @(abc\.)?abstractmethod

    # Don't complain about overload signatures
    @overload
    @typing.overload

    # NOTE: 'pass' and '...' intentionally NOT excluded.
    # Excluding them artificially inflates coverage by hiding empty stubs.

# Show errors for debugging
ignore_errors = false

# Fail under threshold - CI/CD will fail if coverage drops below this
#
# OLCUM (6 Eyl 2026): bu deger 60.0 idi ama GERCEK kapsama 22.27-22.42
# araliginda olculdu (bkz. docs/guvenlik-borcu.md SS10.52/SS10.53).
# Sonuc: kapi her kosumda kapaliydi, Backend Tests master'da 10/10
# kirmizi kaldi. Surekli kirmizi bir kapi hicbir regresyonu yakalayamaz.
#
# 20.0 bir HEDEF DEGIL, olculen tabanin hemen altindaki bir TABAN:
# bugunku durum yesil gecer, ama gercek bir dusus (>2 puan kapsama
# kaybi) kirmizi verir. Kademeli olarak yukseltilecek.
#
# NOT: CI komut satirindan `--cov-fail-under` gecirdigi icin pratikte
# ci.yml'deki deger baskindir; ikisi BILEREK ayni tutuluyor ki yerelde
# `pytest --cov` calistiran da ayni kapiyi gorsun.
fail_under = 20.0

[html]
directory = htmlcov
title = Türkiye Üniversite Sınavları Platform - Test Coverage Report

[json]
output = coverage.json
pretty_print = true
show_contexts = true

# ============================================================================
# MODULE-BASED COVERAGE RULES (Task #30)
# ============================================================================
# Different modules have different coverage requirements.
# These are enforced via CI/CD pipeline validation.
#
# CRITICAL (90%+) - Security-sensitive code:
#   - core/auth*.py
#   - core/jwt_auth.py
#   - core/security*.py
#   - core/data_encryption.py
#   - core/kvkk_compliance.py
#
# HIGH (85%+) - Core algorithms:
#   - algorithms/irt_model.py
#   - algorithms/fsrs*.py
#   - algorithms/turkish_*.py
#   - services/sinav_motoru_service.py
#
# STANDARD (80%+) - Business logic:
#   - services/* (except external integrations)
#   - core/config.py
#   - core/database.py
#
# MODERATE (75%+) - API endpoints:
#   - api/*
#
# BASIC (70%+) - Data models:
#   - models/*
#
# INTEGRATION (60%+) - External integrations:
#   - services/*_client.py
#   - services/*_sync.py
#   - integrations/*
# ============================================================================
