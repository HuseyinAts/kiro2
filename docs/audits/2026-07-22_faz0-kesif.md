# Faz 0 Keşif — ENTEGRASYON_PLANI "önce oku/keşfet" doğrulaması (2026-07-22)

Kaynak: `design/ENTEGRASYON_PLANI.md` + `design/CLAUDE_CODE_TALIMAT.md`.
Yöntem: 4 hedef × (salt-okunur keşif → adversarial doğrulama) paralel workflow.
Her bulgu canlı kodda `dosya:satır` ile kanıtlandı (phantom-filtre disiplini). 8 agent, 0 hata.

> **Amaç:** Planın VAR/YENİ varsayımlarını gerçek repoya karşı netleştirmek. Aşağıdaki tablo,
> plan satırlarının doğrulanmış nihai durumudur.

## Özet — plan satırı → doğrulanmış VAR/YENİ

| Hedef | Plan bölümü | Nihai verdikt | Netleşen |
|---|---|---|---|
| teacher/classes POST | D · Sınıf kur + kod | **VAR-kısmi** | CRUD VAR, katılım kodu + rotate + join + sınıf-varsayılanları **YENİ** |
| Lig ucu | C · Lig (S8) | **VAR-tam** (+1 YENİ) | Lig standings VAR → **yeni `/league` YAZMA**; "sakin mod/gizle" **YENİ** |
| diary_api → mood | G · AI (KARARLAR #5) | **VAR-tam** | mood kayıt ucu + gizlilik guard'ı VAR → **yeniden kurma** |
| bilge_alp.py | G · AI | **VAR-tam** ⚠ düzeltme | Sokratik "evi" bilge_alp DEĞİL → **`enhanced_chat.py`** |

---

## 1) teacher/classes POST şeması — VAR-kısmi

- **VAR:** `GET/POST /api/v1/teacher/classes` (`backend/app/api/teacher_classroom.py:98, :138`, prefix `:43`).
  Request `ClassCreate` (`:51-59`) = name/grade_level/subject_area (TR alias sinif_adi/seviye/ders).
  Response = tipsiz inline dict (`:153-163`): sinif_id/sinif_adi/seviye/ders/ogrenci_sayisi/ortalama_basari.
- **YENİ (doğrulandı):**
  - **Katılım kodu** yok — ne request, ne response, ne DB modeli (`models/teacher_classroom.py:18-28`;
    `alembic/versions/20260410_teacher_classroom_tables.py:27-35` = 6 kolon, `join_code` YOK).
  - `POST /classes/{id}/code/rotate` yok (`grep rotate backend/app/api` = 0 sınıf sonucu).
  - Öğrenci katılım ucu (`POST /me/class/join`) yok — `teacher_classroom_students` tablosuna
    enroll/join handler'ı hiç yok (yalnız analytics salt-okuyor).
  - Sınıf-varsayılanları (sıralama yayınlanmaz / risk bayrağı inmez) sunucu-yazımı yok
    (`create_class :144-149` yalnız 4 alan yazıyor).
- **Sonuç:** Plan D satırı **doğru** — "GET/POST /classes VAR, katilim_kodu yoksa YENİ" varsayımı teyit edildi.

## 2) Lig ucu — VAR-tam (+ "sakin mod" YENİ)

- **VAR:** `GET /api/v1/leagues/current` + `/history` + `POST /award-xp` (`backend/api/league_api.py:72/118/169`,
  `LeagueStandingsResponse` `:34-41`, loader `routers/loader.py:205` kayıtlı). Ayrıca 6 gamification
  leaderboard ucu (`gamification_api.py:530/900/935/971/1003/1064`).
- **Sonuç:** Plan C/S8 "lig ucu backend'de doğrulanır, yoksa YENİ /league" → **YENİ `/league` YAZMA**,
  mevcut `league_api.py` kullanılır.
- **YENİ (ek bulgu):** "sakin mod" / "sıralamayı gizle" opt-out ayarı backend'de **YOK**
  (`show_leaderboard/hide_from_leaderboard/opt_out_ranking/competitive_mode` = 0 sonuç; osb/adhd
  ayarları alakasız — sensory/nefes). Kanon (KARARLAR: rekabet opsiyonel) için opt-out flag +
  servis filtresi (`/current` ve leaderboard uçları is_self-only/boş dönmeli) **YENİ**.

## 3) diary_api.py → mood — VAR-tam

- **VAR:** `POST /api/v1/diary/emotional` ruh-hali KAYIT ucu (`backend/api/diary_api.py:1266`),
  `GET /emotional` `:1288`, `/emotional/trend` `:1320` (`MoodTrendResponse` "Ruh hali trendi"),
  `/emotional/chart` `:1349`. Tablo `emotional_states` (`models/diary.py:359-362`).
- **Gizlilik (KARARLAR #5) ✓:** Tüm mood uçları `current_user.id`'ye kilitli, `_verify_ownership :100`
  → 403; `EmotionalStateCreate` şemasında `user_id` alanı yok (spoof edilemez);
  parent/teacher/veli/öğretmen grep = 0. **Veli/öğretmene sızıntı yok.**
- **Nüans:** `emotional_states` modeli geliştirici/ajan kodlama-mood'u (confidence/flow/frustration),
  öğrenci YKS happy/sad enum'u değil. Öğrenci-YKS mood semantiği istenirse ya bu tablo adapte edilir
  ya da ince `POST /me/mood` eklenir — **ama altyapı (tablo+servis+auth+ownership) hazır, yeniden kurma yok.**

## 4) bilge_alp.py — VAR-tam ⚠ PLAN DÜZELTMESİ

- **bilge_alp NE:** YKS gamification NPC koç (`backend/api/bilge_alp.py`): 12 persona
  (Aristo/Gauss/Farabi…) `NPC_PERSONAS :26-99`; `_build_system_prompt :113-151` persona +
  BKT/ZPD-adaptif system-prompt üretir; `_stream_llm_response :159-231` Claude Haiku
  (`claude-haiku-4-5-20251001`) SSE; uçlar `POST /bilge-alp/chat :239` + `/dialog-options :430`;
  loader `:110` kayıtlı. Mevcut KURALLLAR (`:145-151`) = doğrudan tutor, **katı Sokratik DEĞİL**.
- **⚠ Adversarial doğrulamanın yakaladığı düzeltme:** Sokratik AI sistem-prompt'unun "evi" bilge_alp
  DEĞİL. `backend/api/enhanced_chat.py:207-223` zaten TAM bir `SOCRATIC_SYSTEM_PROMPT` içeriyor
  ("Cevabı ASLA doğrudan verme…") + `_get_system_prompt(subject, teaching_mode)` `:331-337` ile
  `teaching_mode=='socratic'` toggle'ı AKTİF (loader `:100`, `/message :422` + `/stream :538`
  `ChatMessageRequest.teaching_mode` kabul ediyor).
- **Sonuç (Plan G düzeltmesi):** Sokratik iş = `enhanced_chat.py`'de `teaching_mode='socratic'` wire
  etmek (belki sadece bu). `bilge_alp` persona-lezzeti/NPC koç olarak korunur; Sokratik kuralı orada
  ÇOĞALTMA.

---

## Backend'e eklenecek YENİ uçlar (bu keşifle netleşen alt-küme)

- `teacher classes`: `katilim_kodu` kolonu + response alanı · `POST /classes/{id}/code/rotate` ·
  öğrenci `POST /me/class/join {kod}` · sınıf-varsayılanları sunucu-yazımı.
- `lig`: "sakin mod / sıralamayı gizle" kullanıcı ayarı + leaderboard servis filtresi.
- `mood`: (opsiyonel) öğrenci-YKS `POST /me/mood` — yoksa `emotional_states` adaptasyonu.
- `Sokratik`: yeni uç GEREKMEZ — `enhanced_chat.py teaching_mode='socratic'` wire.

*Kanıt dosyaları workflow transcript'inde; her satır canlı kodda grep+read ile doğrulandı.*
