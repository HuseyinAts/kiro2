# P3 — Beta Launch E2E Doğrulama (canlı backend, 2026-06-22)

**Yöntem:** Golden Flow E2E suite (176 test) host'tan canlı backend'e (BACKEND_URL=localhost:8000) koşuldu. Backend `/health`=200, tüm container'lar healthy (3 gün up).
**Sonuç:** **14 PASS / 12 FAIL / 150 SKIP** (skip'ler = ortam-gated, beklenen).

## ⚠️ KRİTİK CAVEAT: Stale container
Çalışan container **3 gün önce** build edildi → bu session'ın commit'leri (F5 Pydantic fix, beta_vp index, → HEAD a3508853b) ve olası diğer fix'ler **DEPLOY EDİLMEMİŞ**. 12 fail'in bir kısmı current-code'da çözülmüş olabilir. Stale-baseline'a güvenilmez (meta-audit dersi). Kesin ayrım için redeploy + re-run şart.

## ÇEKİRDEK ÖĞRENCİ AKIŞI — SAĞLIKLI ✓ (beta'nın kalbi servable)
| GF | Akış | Sonuç |
|---|---|---|
| GF1 | login → /me | PASS |
| GF2 | DAG konular (case-convention, I/ı trap) | PASS |
| GF2wb | placement start → session + soru döner | PASS |
| GF4 | review queue (FSRS read) | PASS |
| GF6 / GF6w | admin question-bank list + soru oluştur | PASS |
| GF5wb | daily quest ilerleme | PASS |
→ **25.343 v_safe havuzu servable; login→placement→sınav-başlat→review→qbank uçtan-uca çalışıyor.**

## 12 FAIL — HEPSİ ÇEVRESEL (öğrenci-sınav çekirdeği DEĞİL)
| GF | Endpoint | Hata | Triyaj |
|---|---|---|---|
| GF5 | teacher profile | 500 | öğretmen paneli |
| GF5w | teacher class create | 500 | "; muhtemelen schema/async |
| GF8wA | kvkk consent list | 500 | **Pattern A sync get_db trap** (golden-flows.md backlog) |
| GF9wA | khan oauth/status | 500 | aynı trap |
| GF9wB | eba watch/history | 500 | aynı trap |
| GF9wC | kvkk privacy export | 500 | dual trap (sync get_db + TokenPayload.id) |
| GF7wA | video-solutions list | 500 | servis init / router wiring |
| GF7 | video fallback (MATEMATIK) | success=false | case-convention (subject_key) |
| GF1x | **logout token invalidation** | 200≠401 | **GÜVENLİK** — logout sonrası /me hâlâ 200. Doğrulanmalı (stale mı?) |
| GF2w | gamification award | 403 | test geçersiz-kaynak gönderiyor → muhtemelen DOĞRU davranış (reason_not_allowed) |
| GF3 | exam-configs list | 404 | test-path drift (gerçek prefix /api/v1/osym-exam; GF2wb sınav-başlatmayı zaten doğruladı) |
| GF150 | clustering health | degraded | opsiyonel ML deps yok (sklearn/hdbscan/umap) — infra, kod değil |

## Verdict (kanıta dayalı)
- **Öğrenci-sınav beta'sı: ÇEKİRDEK HAZIR.** Tam-platform launch: 7 çevresel 500 bloke (çoğu dokümante sync-get_db-trap).
- **Stale-container 12 fail'in kaçının gerçek olduğunu gizliyor** → körlemesine fix YASAK.

## Sıradaki adım (deploy-gate, operatör-döngü)
1. `docker compose build backend && docker compose up -d --no-deps backend` → current HEAD deploy (CLAUDE.md kanonik döngü + .pyc temizle + Start-Sleep 22 + health).
2. E2E re-run → stale-vs-gerçek ayrışır.
3. Kalan gerçek 500'ler TDD'yle fix (Pattern A trap: golden-flows.md + middleware.md reçetesi).
4. GF1x logout-security öncelik (güvenlik).

---
*Test: backend/tests/e2e/test_golden_flows.py -m golden_flow. Çekirdek öğrenci akışı PASS; çevresel entegrasyonlar 500 (stale-container caveat'lı).*
