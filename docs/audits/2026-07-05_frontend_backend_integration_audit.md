# Frontend-Backend Entegrasyon Audit (2026-07-05)

## Methodology

- Araç: `Workflow` (9 paralel domain-map agent + 112 bağımsız doğrulama agent'ı, toplam 121 agent, 1619 tool-call).
- Kapsam: 63 frontend sayfası + core plumbing (apiClient/authService/store), App.tsx'teki 70 route, backend/api/ (158 dosya) + backend/app/api/ (10 dosya) + backend/routers/loader.py.
- Yöntem: her sayfa için route→component→service→gerçek HTTP path→backend/api grep→loader.py kayıt kontrolü zinciri statik olarak (Read/Grep/Glob, sunucuya bağlanılmadan) izlendi.
- Doğrulama: her bulgu, map-agent'ın raporunu kabul etmeyen AYRI bir agent tarafından dosya:satır bazında yeniden okunarak bağımsız doğrulandı (CONFIRMED/PHANTOM/UNCERTAIN).
- Sonuç: 112/112 bulgu CONFIRMED, 0 PHANTOM, 0 UNCERTAIN. Ek olarak ana oturumda 3 P0 bulgu (ModernTeacherDashboard %100 mock, BossFightPage endpoint yokluğu, ModernLoginPage 2FA dead-end) elle spot-check ile bağımsız tekrar doğrulandı.
- Reproducible: `Workflow` run ID `wf_d4d441c5-3df`, journal `subagents/workflows/wf_d4d441c5-3df/journal.jsonl`.

## Özet Sayılar

- Denetlenen alan: 9/9
- Confirmed bulgu: 112 (P0=35, P1=44, P2=33)

## Alan Bazında Durum (Map fazı, doğrulama öncesi status sınıflaması)

| Alan | FULLY_WIRED | PARTIAL | MOCK_ONLY | BROKEN | DEAD_CODE | Toplam |
|---|---|---|---|---|---|---|
| Core Infra (apiClient/authService/store) — KIRO2 frontend | 6 | 1 | 0 | 0 | 0 | 7 |
| Auth & Core Pages (Frontend-Backend Integration) | 4 | 2 | 0 | 0 | 0 | 6 |
| Öğrenci Öğrenme Akışı (Student Learning Flow) | 7 | 1 | 0 | 0 | 0 | 8 |
| Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi | 2 | 2 | 0 | 2 | 0 | 6 |
| Oyunlaştırma & Sosyal (Gamification & Social) | 9 | 3 | 0 | 2 | 0 | 14 |
| Öğretmen paneli (Teacher Dashboard) | 0 | 6 | 1 | 0 | 0 | 7 |
| Veli paneli (+ olası ölü kod) | 0 | 2 | 1 | 1 | 2 | 6 |
| Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi | 3 | 3 | 3 | 5 | 0 | 14 |
| Diğer (chat/profile/settings/a11y) | 0 | 5 | 0 | 0 | 0 | 5 |

## P0 Bulgular (35)

### `frontend/src/components/Admin/AdminPanel.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/panel | Map-status: PARTIAL
- **Bulgu:** Default açılan 'Dashboard' sekmesi (tab index 0) backend response şekli ile crash ediyor — sayfaya girer girmez kırılıyor
- **Kanıt:** components/Admin/AdminDashboard.tsx:74-75 `const dashboardStats = await adminService.getDashboardStats(); setStats(dashboardStats);` sonra satır 175 `{stats.toplam_kullanici.toLocaleString()}` çağrılıyor. Ama backend admin.py:263-271 `return {"success": True, "timestamp": ..., "data": {"kullanicilar": u, "sorular": s, "cat_sessions": c}}` döndürüyor — `toplam_kullanici` alanı hiç yok (asıl alan `kullanicilar.toplam_kullanici`). adminService.ts:199-202 `return response.data;` bu iç içe objeyi olduğu gibi döndürüyor, DashboardStats tipine uymuyor → `stats.toplam_kullanici` undefined, `.toLocaleString()` render'da TypeError fırlatır.
- **Bağımsız doğrulama notu:** Independently verified against live source files (not the prior agent's report).  1. frontend/src/components/Admin/AdminPanel.tsx:145-147 — Tab index 0 (default `currentTab` state) renders `<AdminDashboard />` inside `TabPanel index={0}`, so it mounts immediately on page load, no lazy-render gate.  2. frontend/src/components/Admin/AdminDashboard.tsx:74-75 — `const dashboardStats = await adminService.getDashboardStats(); setStats(dashboardStats);` matches exactly. Line 175 — `value={stats.toplam_...

### `frontend/src/components/Revolutionary/RevolutionaryDashboard.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/labs | Map-status: BROKEN
- **Bulgu:** /admin/labs açıldığında ayar ve istatistik verisi her zaman hardcoded mock'a düşüyor
- **Kanıt:** RevolutionaryDashboard.tsx:106-116 `revolutionaryFeaturesService.getRevolutionarySettings(studentId)` ve `.getRevolutionaryStats(studentId).catch(() => ({fsrs_cards: 24, bionic_texts: 8, ...}))`. Servis tarafında revolutionaryFeaturesService.ts:679 `fetch(`/api/v1/revolutionary-features/settings/${studentId}`)` başarısız olunca satır 702-724 `// Fallback: Mock implementation` ile sabit `mockSettings` dönüyor; backend'de bu path (`/settings/{id}`, `/stats/{id}`) hiç tanımlı değil (grep: sadece `/learning-style/*`, `/zpd-maarif/*`, `/revolutionary/health`).
- **Bağımsız doğrulama notu:** Independently verified every element of the claim with fresh Read/Grep calls (no reliance on the prior agent's report):\n\n1. frontend/src/components/Revolutionary/RevolutionaryDashboard.tsx lines 106-116 exactly match the quoted code: Promise.all of getRevolutionarySettings(studentId) and getRevolutionaryStats(studentId).catch(() => hardcoded stats object).\n2. frontend/src/services/revolutionaryFeaturesService.ts:679 fetches `${baseUrl}/revolutionary-features/settings/${studentId}`; on any fai...

### `frontend/src/pages/ABTestResultsPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/ab-test-results | Map-status: BROKEN
- **Bulgu:** Sayfanın tek veri kaynağı olan endpoint backend'de hiç tanımlı değil — sayfa açılınca crash eder
- **Kanıt:** ABTestResultsPage.tsx:105 `fetch(\`/api/v1/monitoring/ab-test-results?provider=${provider}&days=${timeRange}\`, ...)` — grep ile `ab-test-results`/`ab_test_results` backend/api ve backend/app/api altında sıfır eşleşme. 404 gövdesi `results` olarak set edilir, satır 130 `Object.entries(results.versions)` → `results.versions` undefined → `TypeError: Cannot convert undefined or null to object`.
- **Bağımsız doğrulama notu:** Independently verified against live files, not the prior agent's claim.  1. Frontend: Read frontend/src/pages/ABTestResultsPage.tsx in full. Line 105 is exactly as quoted: `fetch(\`/api/v1/monitoring/ab-test-results?provider=${provider}&days=${timeRange}\`, { credentials: 'include' })`. The response is parsed with `await response.json()` regardless of status code and stored via `setResults(data)` (lines 106-107) — no `response.ok` check. Line 122 only guards against `!results` (i.e., null), so a...

### `frontend/src/pages/ModernAdminContentPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/content | Map-status: BROKEN
- **Bulgu:** İçerik listesi çağrısı yanlış/var olmayan path'e gidiyor, bu yüzden sayfa daima 6 satırlık hardcoded mock veri gösteriyor
- **Kanıt:** ModernAdminContentPage.tsx:82 `const response = await apiClient.get('/api/v1/admin/content');` — admin.py grep sonucunda bare `/content` GET endpoint'i yok (sadece `/content/questions`, `/content/educational`, `/content/search`). Hata catch bloğu satır 84-149 6 kayıtlık `// Mock data` dizisine düşüyor.
- **Bağımsız doğrulama notu:** Independently verified via Read + Grep: 1. frontend/src/pages/ModernAdminContentPage.tsx:82 exactly reads `const response = await apiClient.get('/api/v1/admin/content');` — confirmed verbatim. 2. Grepped `backend/api/admin.py` (router prefix `/api/v1/admin`, confirmed at line 24) for all `@router.get/post/put/delete` declarations: only `/content/questions` (GET/POST), `/content/questions/{id}` (PUT/DELETE), `/content/educational` (GET/POST/PUT/DELETE), `/content/questions/bulk-upload` (POST), `/...

### `frontend/src/pages/ModernAdminContentPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/content | Map-status: BROKEN
- **Bulgu:** Onayla/Reddet butonları hiçbir zaman gerçek backend'e ulaşmıyor
- **Kanıt:** ModernAdminContentPage.tsx:157 `await apiClient.patch(\`/api/v1/admin/content/${contentId}/approve\`);` ve satır 168 `.../reject` — bu path'ler backend'de tanımlı değil; hata durumunda `catch` bloğu sadece `console.error` yapıp state güncellemeden çıkıyor (satır 161-163, 175-177), yani buton tıklamaları sessizce hiçbir şey yapmıyor.
- **Bağımsız doğrulama notu:** Independently verified by reading frontend/src/pages/ModernAdminContentPage.tsx lines 155-178: handleApprove/handleReject call apiClient.patch on `/api/v1/admin/content/${contentId}/approve` and `.../reject`, with catch blocks doing only console.error (no state change, no user feedback) — matches the claim exactly. Cross-checked backend/api/admin.py (the actual router mounted at prefix /api/v1/admin via routers/loader.py) plus content_management.py and content_api.py and openapi.json: none defin...

### `frontend/src/pages/ModernAdminDashboard.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/dashboard | Map-status: MOCK_ONLY
- **Bulgu:** loadDashboardData hiçbir API çağırmıyor, tamamen literal mock veri döndürüyor
- **Kanıt:** ModernAdminDashboard.tsx:87-88: `// Mock data for development` ve `setTimeout(() => { setDashboardData({ sistem_istatistikleri: { toplam_kullanici: 1250, ... } }); setLoading(false); }, 1000);` — dosyada `fetch(`/`apiClient` çağrısı yok.
- **Bağımsız doğrulama notu:** Independently confirmed via direct file read: ModernAdminDashboard.tsx lines 87-133 contain exactly the quoted mock pattern — a `// Mock data for development` comment followed by a setTimeout wrapping hardcoded literal statistics (toplam_kullanici: 1250, etc.), then setLoading(false). Grepped the entire file for fetch(/apiClient/axios/api.get/useQuery — zero matches, confirming no network call exists anywhere in the component. Confirmed the route is live (App.tsx:59 lazy-imports this exact file ...

### `frontend/src/pages/ModernAdminSettingsPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/settings | Map-status: MOCK_ONLY
- **Bulgu:** Kaydet başarısız olsa bile kullanıcıya 'başarıyla kaydedildi' mesajı gösteriliyor (fake-success)
- **Kanıt:** ModernAdminSettingsPage.tsx:74-93 `const response = await fetch('/api/v1/admin/settings', {method:'PUT',...}); if (!response.ok) {throw new Error();} setSaved(true); ... } catch { // Simulate save setSaved(true); setTimeout(() => setSaved(false), 3000); }` — endpoint backend'de hiç yok, yani her tıklama catch'e düşüyor ve yine de başarı mesajı basılıyor.
- **Bağımsız doğrulama notu:** Independently verified. Read frontend/src/pages/ModernAdminSettingsPage.tsx lines 74-93: the code matches the cited snippet exactly — handleSave() does `fetch('/api/v1/admin/settings', {method:'PUT',...})`, throws on `!response.ok`, and on success sets `setSaved(true)`. Critically, the `catch` block (line 88-92) contains the comment `// Simulate save` followed by `setSaved(true); setTimeout(() => setSaved(false), 3000);` — meaning ANY failure (network error, 404, 500, thrown Error from !response...

### `frontend/src/pages/ModernAdminUsersPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/users | Map-status: PARTIAL
- **Bulgu:** Yeni kullanıcı oluşturma her zaman 501 ile başarısız oluyor
- **Kanıt:** ModernAdminUsersPage.tsx:155 `await apiClient.post('/api/v1/admin/users', newUser);` ama backend admin.py:101-113 `raise HTTPException(status_code=status.HTTP_501_NOT_IMPLEMENTED, detail="Kullanici olusturmak icin /api/v1/auth/kayit endpoint'ini kullanin.")`
- **Bağımsız doğrulama notu:** Independently verified both sides of the claim with fresh Read/Grep:  1. Frontend: frontend/src/pages/ModernAdminUsersPage.tsx:155 does call `await apiClient.post('/api/v1/admin/users', newUser);` inside `handleCreateUser`, with only a generic alert on failure (no fallback to /auth/kayit).  2. Backend: backend/api/admin.py has `router = APIRouter(prefix="/api/v1/admin", ...)` (line 24) and `@router.post("/users", ...)` `kullanici_olustur` (lines 101-113), which unconditionally raises `HTTPExcept...

### `frontend/src/pages/ModernAdminUsersPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/users | Map-status: PARTIAL
- **Bulgu:** Kullanıcı durumu değiştirme (Aktif/Pasif Yap) çağırdığı path backend'de yok
- **Kanıt:** ModernAdminUsersPage.tsx:195 `await apiClient.patch(\`/api/v1/admin/users/${selectedUser.id}/status\`, { durum: newStatus });` — admin.py içinde `@router.patch` hiç kullanılmıyor (sadece GET/POST/PUT/DELETE var), `/status` alt-yolu tanımlı değil.
- **Bağımsız doğrulama notu:** Independently verified both sides of the claim.  Frontend: frontend/src/pages/ModernAdminUsersPage.tsx:195 (function handleToggleStatus, lines 191-204) does exactly what was claimed: ``` await apiClient.patch(`/api/v1/admin/users/${selectedUser.id}/status`, { durum: newStatus }); ```  Backend: backend/api/admin.py (router = APIRouter(prefix="/api/v1/admin", ...)) only defines these routes: GET /users, POST /users, GET /users/{...}, PUT /users/{kullanici_id}, DELETE /users/{kullanici_id}, GET /da...

### `frontend/src/pages/OSYMQuestionGeneratorPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/osym-generator | Map-status: BROKEN
- **Bulgu:** Frontend'in çağırdığı path backend router prefix'i ile eşleşmiyor — soru üretme her zaman 404 veriyor
- **Kanıt:** OSYMQuestionGeneratorPage.tsx:96 `const response = await fetch('/api/v1/osym/generate-question', {method:'POST', ...});` ama osym_routes.py:23 `router = APIRouter(prefix="/api/v1/osym/generate", ...)` ve satır 60 `@router.post("/generate-question")` → gerçek tam path `/api/v1/osym/generate/generate-question`. Frontend'de eksik olan segment `/generate`.
- **Bağımsız doğrulama notu:** Independently verified all cited evidence: 1. frontend/src/pages/OSYMQuestionGeneratorPage.tsx:96 does contain `fetch('/api/v1/osym/generate-question', {method:'POST', ...})` exactly as claimed. 2. backend/api/osym_routes.py:23 does contain `router = APIRouter(prefix="/api/v1/osym/generate", tags=["OSYM Question Generation"])` and line 60 has `@router.post("/generate-question")`, making the real full path `/api/v1/osym/generate/generate-question`. 3. Ruled out phantom possibilities: grepped the ...

### `frontend/src/pages/SystematicDebuggingPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/debug | Map-status: MOCK_ONLY
- **Bulgu:** 'Altyapı Kontrolü' özelliği gerçek bir sağlık kontrolü yapmıyor, sonucu hardcoded 'ok' olarak simüle ediyor
- **Kanıt:** SystematicDebuggingPage.tsx:471-489 `const runInfraCheck = async () => { ... const statuses: InfraStatus[] = ['ok', 'ok', 'ok', 'ok']; for (let i = 0; i < INFRA_INITIAL.length; i++) { await new Promise<void>(r => setTimeout(r, delays[i])); const status = statuses[i]; setInfra(...); addLog(status === 'ok' ? 'PASS' : 'ERROR', ...); } }` — statuses dizisi sabit kodlanmış, gerçek Docker/PostgreSQL/Redis durumuna hiçbir şekilde bakılmıyor; sayfanın hiçbir yerinde network çağrısı yok.
- **Bağımsız doğrulama notu:** Read frontend/src/pages/SystematicDebuggingPage.tsx lines 471-489 directly and confirmed the exact code quoted in the finding: `runInfraCheck` sets `const statuses: InfraStatus[] = ['ok', 'ok', 'ok', 'ok'];` (hardcoded, line 478) and then only uses `setTimeout` delays (line 481) to simulate progressive status updates before writing these fixed statuses into state and logging them as PASS. There is no branching on any external result.  Additionally grepped the entire file for `fetch(`, `axios`, `...

### `frontend/src/pages/TokenOptimizationDashboard.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/token-dashboard | Map-status: BROKEN
- **Bulgu:** Ana istatistik endpoint'i (token-stats) backend'de yok; sayfa 404 gövdesini stats olarak set edip render'da crash ediyor
- **Kanıt:** TokenOptimizationDashboard.tsx:89 `fetch(\`/api/v1/monitoring/token-stats?days=${timeRange}\`, ...)` — monitoring.py içinde grep sonucu sadece `/token-projection` (satır 437) var, `/token-stats` tanımlı değil. `fetch` 404'te reject etmediği için `statsData = await statsRes.json()` FastAPI'nin `{"detail":"Not Found"}` gövdesini alır, `setStats(statsData)` sonrası satır 135 `stats.average_savings_percentage.toFixed(1)` ve satır 136 `Object.entries(stats.provider_breakdown)` undefined üzerinde çağrılıp TypeError fırlatır.
- **Bağımsız doğrulama notu:** Independently verified against live files, not just the prior agent's claim.  1. Frontend source exact match: `frontend/src/pages/TokenOptimizationDashboard.tsx:89` calls `fetch(\`/api/v1/monitoring/token-stats?days=${timeRange}\`, ...)`. Lines 135-136 then do `stats.average_savings_percentage.toFixed(1)` and `Object.entries(stats.provider_breakdown)` with no null/shape guard beyond the initial `!stats` check (line 127), and `stats` is set unconditionally from `await statsRes.json()` regardless ...

### `frontend/src/pages/ModernLoginPage.tsx` — Auth & Core Pages (Frontend-Backend Integration)
- Route: /login | Map-status: PARTIAL
- **Bulgu:** 2FA login flow is a dead end — page ignores the '2fa_required' signal, leaving TOTP-enabled users stuck with no error and no challenge UI
- **Kanıt:** ModernLoginPage.tsx:82-85: `const success = await login(formData); if (!success) { setError('E-posta veya şifre hatalı'); }` — '2fa_required' is a truthy string so `!success` is false, no error shown and no navigation happens. authStore.ts:180-192 explicitly returns '2fa_required' and stashes `window.__pending2faEmail` "so ModernLoginPage can route to the 2FA challenge step" but no code reads `__pending2faEmail` anywhere in frontend/src (grep confirms single write, zero reads), and no /2fa or /totp route exists in App.tsx. Backend confirms the feature is real: `backend/api/auth.py:827` and `:918` both set `"requires_2fa": True`.
- **Bağımsız doğrulama notu:** Independently verified all claims:  1. ModernLoginPage.tsx:82-85 reads exactly as claimed: ``` const success = await login(formData); if (!success) {   setError('E-posta veya şifre hatalı'); } ``` Since `success` can be `boolean | '2fa_required'`, and `'2fa_required'` is a truthy non-empty string, `!success` evaluates to `false` when 2FA is required — so no error is shown, and since `isAuthenticated` is never set to true in this path either, the `useEffect` redirect never fires. The user is left...

### `frontend/src/pages/ModernChatPage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /chat | Map-status: PARTIAL
- **Bulgu:** Primary send-message flow (streaming) hardcodes student_id='current' which fails the backend IDOR ownership check for every real authenticated student
- **Kanıt:** frontend/src/services/chatService.ts:113 `body: JSON.stringify({ student_id: 'current', message, session_id: this.sessionId, ... })` posted to POST /api/v1/enhanced-chat/stream (called from frontend/src/components/Chat/TurkishChatInterface.tsx:182 `await chatService.sendMessageStreaming(text, ...)`, which is the handler used by ModernChatPage.tsx:112 `<TurkishChatInterface studentId={user?.id || 'anonymous'} />` -> handleSubmit). Backend backend/api/enhanced_chat.py:548-550 calls `await _verify_enhanced_chat_student_context(payload.student_id, current_user, db)`, which for non-privileged users (route /chat is ProtectedRoute requiredRoles=['ogrenci'], so current_user is always a student) executes backend/core/learning_path_auth.py:120-135 `select(LearningPathStudentProfile.student_id).where(..., LearningPathStudentProfile.student_id == student_id)` with student_id literally 'current' -> no row named 'current' exists -> `raise HTTPException(403, 'Bu öğrenci verisine erişim yetkiniz yok')`. If db is unavailable the same guard raises 503 instead (enhanced_chat.py:94-98). Either way, every authenticated student's chat message send fails.
- **Bağımsız doğrulama notu:** Independently verified every link in the claimed chain:  1. frontend/src/services/chatService.ts:108-119 (sendMessageStreaming) — confirmed body hardcodes `student_id: 'current'` (line 113) regardless of the actual logged-in student's real ID. This function takes no studentId parameter at all.  2. frontend/src/components/Chat/TurkishChatInterface.tsx — the `studentId` prop is destructured as `studentId: _studentId` (line 85), the underscore-prefix rename signaling it is intentionally unused. `ha...

### `frontend/src/pages/ModernChatPage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /chat | Map-status: PARTIAL
- **Bulgu:** Attachment/photo upload in chat never sends student_id, causing a guaranteed 400 for authenticated users
- **Kanıt:** frontend/src/services/chatService.ts:195-201 builds FormData with file/url/message/subject/session_id/teaching_mode but never appends `student_id` (called from TurkishChatInterface.tsx:263 `chatService.sendMessageWithAttachment({ file: attachment, message: text, subject, teachingMode })`). Backend backend/api/enhanced_chat.py:889-897 `student_id: str = Form(default="")` then `if current_user is not None and not (student_id or "").strip(): raise HTTPException(status_code=400, detail="student_id gerekli (kimliği doğrulanmış istekler için)")`. Since student_id is always empty from the frontend, every attachment-based chat message fails with 400 for logged-in users.
- **Bağımsız doğrulama notu:** Independently verified all cited code paths.  1. frontend/src/services/chatService.ts:184-201 — `sendMessageWithAttachment(options: { file?, url?, message?, subject?, teachingMode? })` builds `FormData` with `file`, `url`, `message`, `subject`, `session_id`, `teaching_mode`. There is no `student_id` field in the options type at all, nor is it appended to FormData anywhere in the function body.  2. frontend/src/components/Chat/TurkishChatInterface.tsx:263-268 — the only caller invokes `chatServic...

### `frontend/src/pages/BossFightPage.tsx` — Oyunlaştırma & Sosyal (Gamification & Social)
- Route: /boss-fight/:realmSlug (App.tsx:381) | Map-status: BROKEN
- **Bulgu:** Frontend, backend'de hiç var olmayan '/api/v1/soru-bankasi/random' path'ini çağırıyor — boss fight hiçbir zaman soru yükleyemiyor.
- **Kanıt:** BossFightPage.tsx:59-61: "apiRequest<{ questions?: BossQuestion[]; data?: BossQuestion[] }>(`/api/v1/soru-bankasi/random?subject_area=${subject}&limit=10&difficulty=hard`,)". grep 'soru-bankasi/random' ve '"/random"' backend/api/*.py + backend/app/api/*.py içinde 0 eşleşme verdi.
- **Bağımsız doğrulama notu:** Verified independently: 1. frontend/src/pages/BossFightPage.tsx:59-61 does call `apiRequest(`/api/v1/soru-bankasi/random?subject_area=${subject}&limit=10&difficulty=hard`)` exactly as claimed — confirmed by direct Read. 2. Grepped backend/api/*.py and backend/app/api/*.py (and backend/ broadly) for "soru-bankasi/random", "/random" combined with soru-bankasi — zero matches for that exact path. 3. Checked for phantom/rename possibilities per audit-methodology.md: found only one `/random` route, in...

### `frontend/src/pages/BossFightPage.tsx` — Oyunlaştırma & Sosyal (Gamification & Social)
- Route: /boss-fight/:realmSlug (App.tsx:381) | Map-status: BROKEN
- **Bulgu:** backend/api/soru_bankasi.py router'ında prefix tanımlı değil (sibling router'ların hepsinden farklı) — en yakın gerçek endpoint '/rastgele-sorular' bile '/api/v1/soru-bankasi' altında değil, çıplak path'te mount ediliyor; ayrıca farklı query param ve response şeması bekliyor.
- **Kanıt:** soru_bankasi.py:27 'router = APIRouter(tags=["Soru Bankası"])' (prefix argümanı yok) vs. gamification_api.py:36 'router = APIRouter(prefix="/api/v1/gamification", tags=["Gamification"])', realms.py:29, oba_api.py:26, social_summary_api.py:18 hepsi prefix veriyor. routers/loader.py:283-289 raw_prefix boşsa sadece log etiketi üretiyor, gerçek include_router(router) (loader.py:313) hiçbir prefix eklemiyor. Ayrıca soru_bankasi.py:250-253 gerçek endpoint 'sinav_tipi' ve 'soru_sayisi' (subject_area/limit/difficulty DEĞİL) parametreleri istiyor, yanıtı da soru_bankasi.py:337-346 'data': {'sorular': [...]} şeklinde nested — BossFightPage'in 'res.data' üzerinde Array.isArray kontrolü (satır 63-64) bu şekilde de fail eder.
- **Bağımsız doğrulama notu:** Independently verified every cited fact:  1. backend/api/soru_bankasi.py:27 — `router = APIRouter(tags=["Soru Bankası"])` confirmed, no prefix arg. 2. Sibling routers all declare prefix: gamification_api.py:36 `prefix="/api/v1/gamification"`, realms.py:29 `prefix="/api/v1/realms"`, oba_api.py:26 `prefix="/api/v1/oba"`, social_summary_api.py:18 `prefix="/api/v1/social"` — all confirmed exact line/content matches. 3. routers/loader.py:283-289 — confirmed exact logic: `raw_prefix = getattr(router, ...

### `frontend/src/pages/CozumDuellosuPage.tsx` — Oyunlaştırma & Sosyal (Gamification & Social)
- Route: /cozum-duellosu (App.tsx:430) | Map-status: BROKEN
- **Bulgu:** Frontend 'Yeni Duello' aksiyonu, backend'in artık kesinlikle reddettiği sabit literal 'auto' değerini question_bank_id olarak gönderiyor — duel oluşturma her zaman HTTP 422 ile başarısız oluyor.
- **Kanıt:** CozumDuellosuPage.tsx:65-68: 'const res = await cozumDuellosu.create({ question_bank_id: \'auto\', subject_area: subject.toUpperCase(), });'. backend/api/cozum_duellosu_api.py:100-121: '# S179 fix (B-P0-42): question_bank_id must exist in question_bank. Pre-fix the literal string "auto" (or any other arbitrary value) was stored, leaving voters unable to ever load the soru.' ... 'raise HTTPException(status_code=422, detail={"error": "question_bank_id_invalid", "message": ("Geçerli bir aktif soru ID'si gerekli. " "'auto' veya rastgele literal kabul edilmez.")})'. Backend commit S179 'auto' literalini bilerek kırdı ama frontend hiç güncellenmemiş.
- **Bağımsız doğrulama notu:** Independently verified both sides. Frontend: frontend/src/pages/CozumDuellosuPage.tsx lines 65-68 (handleCreate) literally sends `question_bank_id: 'auto'` to `cozumDuellosu.create(...)`. Backend: backend/api/cozum_duellosu_api.py lines 100-121 contain the S179 fix comment referencing B-P0-42, and code that queries `QuestionBankItem` for `id == body.question_bank_id` with `is_active == True`; if no match, raises HTTPException 422 with error `question_bank_id_invalid` and message explicitly stati...

### `frontend/src/pages/ModernExamHistoryPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /exam/history (App.tsx:258) AND /exams (App.tsx:282-288, same ExamHistoryPage component confirmed by reading App.tsx:281-288) | Map-status: PARTIAL
- **Bulgu:** Backend returns a bare JSON array but frontend reads data.exams, so real successful responses always render an empty list
- **Kanıt:** frontend/src/pages/ModernExamHistoryPage.tsx:66-67: "const data = await response.json();\n      setExams(data.exams || []);" — backend/api/sinav.py:263-264: "@router.get(\n    \"/my-exams\", response_model=list[ExamSessionResponse], ..." returns a top-level list, so data.exams is always undefined on success.
- **Bağımsız doğrulama notu:** Independently verified all cited lines. frontend/src/pages/ModernExamHistoryPage.tsx:62 calls fetch('/api/v1/osym-exam/my-exams', {credentials:'include'}); lines 66-67 are exactly 'const data = await response.json(); setExams(data.exams || []);'. backend/api/sinav.py:24 sets router prefix '/api/v1/osym-exam', and lines 263-270 define '@router.get(\"/my-exams\", response_model=list[ExamSessionResponse], ...)' returning list[ExamSessionResponse] — a bare JSON array on success, not an object with a...

### `frontend/src/pages/PhotoAskPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /photo-ask (App.tsx:290, lazy import App.tsx:88) | Map-status: BROKEN
- **Bulgu:** Upload response field name mismatch causes a hard crash on every successful photo upload
- **Kanıt:** backend/api/photo_ask_api.py:47-53 PhotoAskResponse defines "matched_questions: list[MatchedQuestion]" (no similar_questions field). Frontend renders "uploadResult.similar_questions.length > 0" at frontend/src/pages/PhotoAskPage.tsx:310 with no optional chaining — since similar_questions is undefined on the real response, this throws TypeError: Cannot read properties of undefined (reading 'length') in the Results-state render.
- **Bağımsız doğrulama notu:** Bağımsız doğrulama tamamlandı:  1. frontend/src/pages/PhotoAskPage.tsx:46 — `UploadResult` arayüzü `similar_questions: SimilarQuestion[]` alanını tanımlıyor. 2. Satır 310: `uploadResult.similar_questions.length > 0` — optional chaining (`?.`) YOK, doğrudan erişim. 3. backend/api/photo_ask_api.py:47-52 — `PhotoAskResponse` gerçekten `matched_questions: list[MatchedQuestion]` alanını dönüyor; `similar_questions` alanı backend response modelinde YOK. 4. handleUpload (satır 101-128): `fetch('/api/v1...

### `frontend/src/pages/QuestionUploadPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /question-upload (App.tsx:690, lazy import App.tsx:71) | Map-status: BROKEN
- **Bulgu:** yoloService builds request URLs without the /api/v1 prefix, so every AI-detection call misses the backend entirely in both dev and prod
- **Kanıt:** frontend/src/services/yoloService.ts:135: "this.baseUrl = `${API_BASE_URL}/yolo`;" produces paths like /yolo/detect, but the real backend router is mounted at prefix="/api/v1/yolo" (backend/api/yolo_detection_api.py:53). Production nginx (frontend/nginx.conf) only proxies location blocks /api/, /api/v1/enhanced-chat/stream, /api/v1/stream/, /api/v1/streaming/, /static/, /health, /healthz to the backend — /yolo/* falls through to the SPA fallback "location / { try_files $uri $uri/ /index.html; }" (frontend/nginx.conf:111-113). Vite dev server proxy (frontend/vite.config.ts:224-235) only proxies '/api' and '/static', not '/yolo'. QuestionUploadPage renders YOLOQuestionDetector (frontend/src/pages/QuestionUploadPage.tsx:155-160), which calls yoloService.detectQuestions()/cropQuestions() exclusively — the page's entire core feature (AI question detection) cannot reach the backend.
- **Bağımsız doğrulama notu:** Independently verified every claim: (1) frontend/src/services/yoloService.ts:135 literally reads `this.baseUrl = `${API_BASE_URL}/yolo`;` — confirmed exact match. (2) backend/api/yolo_detection_api.py:53 confirms `prefix=\"/api/v1/yolo\"`, and backend/routers/loader.py:313 shows `self.app.include_router(router)` with no extra prefix applied — so the router is genuinely mounted at `/api/v1/yolo/*`, not `/yolo/*` (ruled out phantom-prefix explanation). (3) frontend/src/config/index.ts shows `confi...

### `frontend/src/pages/ModernParentDashboard.tsx` — Veli paneli (+ olası ölü kod)
- Route: /parent/dashboard | Map-status: MOCK_ONLY
- **Bulgu:** Sayfa hiçbir zaman backend'e istek atmıyor; tüm içerik (çocuklar, aktiviteler, etkinlikler, öğretmen mesajları) component içinde literal hardcoded array.
- **Kanıt:** ModernParentDashboard.tsx:42-76 `// Mock data - gerçek API'den gelecek\n  const children = [ { id: 1, name: 'Ahmet Yılmaz', ... } ]`; dosyada `fetch(` veya `apiClient.` çağrısı YOK (grep sonucu 0 eşleşme). Buna karşın backend/api/parent.py:267 `GET /dashboard` gerçek DB'den (`ParentService.get_parent_dashboard_data`) veri döndürüyor ve loader.py:117'de kayıtlı — hiç kullanılmıyor.
- **Bağımsız doğrulama notu:** Bağımsız doğrulama: ModernParentDashboard.tsx:42-76 satırlarında iddia edilen mock data (children, recentActivities, upcomingEvents, teacherMessages) birebir mevcut. Dosyada fetch(/apiClient./axios./useEffect için grep 0 eşleşme verdi — gerçekten hiçbir API çağrısı yok. App.tsx:53,546-549 üzerinden bu component /parent/dashboard route'una bağlı (lazy-loaded ParentDashboardPage) — yani ölü kod değil, canlı ve kullanıcıya sunulan sayfa. Backend tarafında backend/api/parent.py:267 GET /dashboard en...

### `frontend/src/pages/ModernParentNotificationsPage.tsx` — Veli paneli (+ olası ölü kod)
- Route: /parent/notifications | Map-status: PARTIAL
- **Bulgu:** 'Okundu işaretle' HTTP method uyuşmazlığı: frontend PATCH atıyor, backend sadece PUT tanımlı -> 405 Method Not Allowed.
- **Kanıt:** ModernParentNotificationsPage.tsx:210 `await apiClient.patch(\`/api/v1/parent/notifications/${id}/read\`);` vs backend/api/parent.py:234 `@router.put("/notifications/{notification_id}/read")`. FastAPI kayıtlı olmayan method için 405 döner; tıklama sessizce başarısız olur (catch sadece console.error, satır 214-216).
- **Bağımsız doğrulama notu:** Independently verified all claimed facts: 1. Frontend: ModernParentNotificationsPage.tsx:210 calls `await apiClient.patch(`/api/v1/parent/notifications/${id}/read`)` inside handleMarkAsRead, with catch block at lines 214-216 doing only `console.error(...)` — confirmed exact match. 2. apiClient.patch (frontend/src/services/apiClient.ts:184) is a thin wrapper around axios `this.client.patch<T>(...)`, so it genuinely issues an HTTP PATCH request (not a mislabeled PUT). 3. Backend: backend/api/paren...

### `frontend/src/pages/ModernParentNotificationsPage.tsx` — Veli paneli (+ olası ölü kod)
- Route: /parent/notifications | Map-status: PARTIAL
- **Bulgu:** 'Tümünü Okundu İşaretle' butonu var olmayan bir endpoint'i çağırıyor.
- **Kanıt:** ModernParentNotificationsPage.tsx:221 `await apiClient.patch('/api/v1/parent/notifications/read-all');` -- grep backend/api/*.py backend/app/api/*.py: 'read-all' hiçbir yerde tanımlı değil. Her tıklama 404 ile başarısız olur, kullanıcıya hata gösterilmez (sadece console.error, satır 223-225).
- **Bağımsız doğrulama notu:** Verified frontend/src/pages/ModernParentNotificationsPage.tsx:221 contains exactly `await apiClient.patch('/api/v1/parent/notifications/read-all');` inside handleMarkAllAsRead (lines 219-226), which is wired to a real button (`onClick={handleMarkAllAsRead}` at line 316). Failures are swallowed into `console.error` only (line 224), with no user-facing error surfaced — matches the claim.  Backend verification: grepped `backend/`, `backend/api/`, and `backend/app/api/` for "read-all" — zero matches...

### `frontend/src/pages/ModernParentNotificationsPage.tsx` — Veli paneli (+ olası ölü kod)
- Route: /parent/notifications | Map-status: PARTIAL
- **Bulgu:** Bildirim silme (çöp kutusu ikonu) var olmayan DELETE endpoint'ini çağırıyor.
- **Kanıt:** ModernParentNotificationsPage.tsx:230 `await apiClient.delete(\`/api/v1/parent/notifications/${id}\`);` -- backend/api/parent.py içinde notifications için hiçbir DELETE route tanımı yok (sadece POST, GET, PUT /read mevcut). 404 -> silme her zaman başarısız, local state değişmiyor ama kullanıcı hata görmüyor (satır 232-234 sadece console.error).
- **Bağımsız doğrulama notu:** Independently verified via Read and Grep: 1. frontend/src/pages/ModernParentNotificationsPage.tsx:230 exactly matches the quoted line: `await apiClient.delete(\`/api/v1/parent/notifications/${id}\`);` inside handleDelete (lines 228-235). On failure it only does `console.error('Bildirim silinemedi:', error)` (line 233) with no user-facing error surfaced — matches the claim. 2. backend/api/parent.py has router prefix `/api/v1/parent` (line 21) and defines only: POST /notifications (line 165), GET ...

### `frontend/src/pages/ModernParentReportsPage.tsx` — Veli paneli (+ olası ölü kod)
- Route: /parent/reports | Map-status: BROKEN
- **Bulgu:** fetchChildren() var olmayan '/api/v1/parent/children-performance' endpoint'ini çağırıyor; backend'de sadece '/children' (liste) ve '/children/{id}/performance' (tekil) var, ikisi de bu path ile eşleşmiyor.
- **Kanıt:** ModernParentReportsPage.tsx:79 `const response = await apiClient.get('/api/v1/parent/children-performance');` -- grep backend/api/parent.py + backend/app/api/*.py: 0 eşleşme 'children-performance' için. Sonuç: her istek 404, catch bloğu (satır 87-104) hep mock veriye düşüyor.
- **Bağımsız doğrulama notu:** Independently verified via Read + Grep, not by trusting the prior report.  1. File/line check: frontend/src/pages/ModernParentReportsPage.tsx line 79 reads exactly `const response = await apiClient.get('/api/v1/parent/children-performance');` inside `fetchChildren()` (lines 76-107). The catch block (lines 85-106) does set mock/dummy data (`Ahmet Yılmaz`, hardcoded stats) on any error, confirming the "falls back to mock data" claim.  2. Backend endpoint check: `backend/api/parent.py` is the only ...

### `frontend/src/pages/ModernParentReportsPage.tsx` — Veli paneli (+ olası ölü kod)
- Route: /parent/reports | Map-status: BROKEN
- **Bulgu:** fetchWeeklyReports() var olmayan '.../weekly-performance' path'ini çağırıyor; backend'deki gerçek endpoint adı 'weekly-report' (tekil 'report', 'performance' değil).
- **Kanıt:** ModernParentReportsPage.tsx:111 `const response = await apiClient.get(\`/api/v1/parent/children/${childId}/weekly-performance\`);` vs backend/api/parent.py:125 `@router.get("/children/{child_id}/weekly-report", ...)`. Path adı uyuşmuyor -> 404, catch (satır 113-150) mock array'e düşüyor.
- **Bağımsız doğrulama notu:** Independently verified via Read + Grep. frontend/src/pages/ModernParentReportsPage.tsx:111 literally calls `/api/v1/parent/children/${childId}/weekly-performance`. backend/api/parent.py declares router prefix `/api/v1/parent` (line 21) and the only matching route is `@router.get("/children/{child_id}/weekly-report", ...)` at line 125 - full path `/api/v1/parent/children/{child_id}/weekly-report`, confirmed also in backend/openapi.json:34281. A repo-wide grep for weekly-performance/weekly_perform...

### `frontend/src/pages/ModernLearningPathPage.tsx` — Öğrenci Öğrenme Akışı (Student Learning Flow)
- Route: /learning-path | Map-status: PARTIAL
- **Bulgu:** useLearningPath.ts calls '/api/learning-path/my-profile' (missing /v1/) which 404s against every registered router; the catch-all fallback then silently calls createStudentProfile, so a NEW student profile is created on every single page mount/reload instead of reusing the existing one.
- **Kanıt:** frontend/src/hooks/useLearningPath.ts:122-143 `const myProfile = await apiRequest<{ student_id: string }>('/api/learning-path/my-profile'); ... catch (profileErr) { if (...401.../Oturum...) throw; const profile = await createStudentProfile({...}); return profile.student_id; }` — the only backend file exposing this exact sub-path without /v1/ is backend/api/learning_path.py:288 (`router = APIRouter(prefix="/api/learning-path", ...)` at line 99), and that module is not present anywhere in backend/routers/loader.py's ROUTER_MAPPING nor in backend/main.py include_router calls (verified via project-wide grep).
- **Bağımsız doğrulama notu:** Independently verified every claim in the finding against live source:  1. frontend/src/hooks/useLearningPath.ts:122-143 — exact match to the quoted code. `ensureProfile()` calls `apiRequest('/api/learning-path/my-profile')`, catches any non-401 error and falls through to `createStudentProfile(...)`, returning a brand-new profile's student_id. Confirmed this runs on every mount via `loadPath()` (called in useEffect at lines 297 and 439-442), which ModernLearningPathPage.tsx invokes through `useL...

### `frontend/src/pages/ModernLearningPathPage.tsx` — Öğrenci Öğrenme Akışı (Student Learning Flow)
- Route: /learning-path | Map-status: PARTIAL
- **Bulgu:** Progress persistence (PUT /api/learning-path/progress/{sid}/{nodeId}) always 404s, so node-completion/progress updates from markNodeComplete/updateProgress never reach the backend — user progress silently does not save across sessions.
- **Kanıt:** frontend/src/hooks/useLearningPath.ts:317 `await apiRequest(`/api/learning-path/progress/${sid}/${nodeId}`, { method: 'PUT', ... })` vs registered path `PUT /api/v1/learning-path/progress/{student_id}/{node_id}` at backend/api/learning_path_v2.py:1477.
- **Bağımsız doğrulama notu:** Independently verified every link in the claim:\n1. frontend/src/hooks/useLearningPath.ts:317 does call `apiRequest(`/api/learning-path/progress/${sid}/${nodeId}`, { method: 'PUT', ... })` exactly as quoted.\n2. apiRequest() in frontend/src/utils/apiHelpers.ts:432-478 is a raw `fetch(url, ...)` — it does not prepend any base URL or /v1 prefix.\n3. Vite dev proxy (frontend/vite.config.ts:224-231) forwards `/api/*` to `http://localhost:8000` verbatim, no path rewriting, so the request path stays `...

### `frontend/src/pages/ModernLearningPathPage.tsx` — Öğrenci Öğrenme Akışı (Student Learning Flow)
- Route: /learning-path | Map-status: PARTIAL
- **Bulgu:** Study session timer feature ('Oturum Başlat'/'Bitir' buttons) is completely non-functional: POST /api/learning-path/study-session/start and /end match no registered route anywhere in the backend.
- **Kanıt:** frontend/src/hooks/useLearningPath.ts:372-386 `await apiRequest<{...}>('/api/learning-path/study-session/start', { method: 'POST' })` and lines 389-418 for '/study-session/end'; wired to UI buttons in frontend/src/pages/ModernLearningPathPage.tsx:522-542 ('Oturum Başlat' / 'Bitir'). The matching path exists only in the dead backend/api/learning_path.py:1553,1611 — never registered. The registered FSRS module uses a different path entirely: /api/v1/fsrs/study-sessions/start (plural) in backend/app/api/fsrs.py:397.
- **Bağımsız doğrulama notu:** Independently verified every cited line. frontend/src/hooks/useLearningPath.ts:372-386 and 389-418 call POST /api/learning-path/study-session/start and /end exactly as claimed. frontend/src/pages/ModernLearningPathPage.tsx:522-542 wires these via startSession/endSession to the 'Oturum Başlat'/'Bitir' buttons exactly as claimed. backend/api/learning_path.py:1553,1611 do define matching routes under router prefix /api/learning-path (line 99), confirming the path would match IF registered. However,...

### `frontend/src/pages/ModernLearningPathPage.tsx` — Öğrenci Öğrenme Akışı (Student Learning Flow)
- Route: /learning-path | Map-status: PARTIAL
- **Bulgu:** Node quiz ('Başla' on a path node), Productive-Failure flow, FSRS wrong-answer registration, and 'Karışık Pratik' (interleaved practice) all fail silently because their fetch() calls omit the /v1/ prefix and match no registered backend route.
- **Kanıt:** frontend/src/pages/ModernLearningPathPage.tsx:214-217 `fetch(`/api/learning-path/exit-quiz/${encodeURIComponent(subject)}?count=...`, ...)`; line 265 and 759 `fetch('/api/learning-path/register-wrong-answers', ...)`; line 788 `fetch(`/api/learning-path/interleaved-practice?subjects=...`, ...)`. Registered equivalents (with /v1/) exist at backend/api/learning_path_v2.py:1860, 2036, 1904 respectively, but the called URLs match only the unregistered backend/api/learning_path.py.
- **Bağımsız doğrulama notu:** Independently verified all cited evidence:  1. Frontend (frontend/src/pages/ModernLearningPathPage.tsx): confirmed exact content at claimed lines.    - Line 214-217: fetch(`/api/learning-path/exit-quiz/${encodeURIComponent(subject)}?count=...`) — node quiz start    - Line 233-236: same URL pattern for Productive Failure flow    - Line 265 and 759: fetch('/api/learning-path/register-wrong-answers', ...) — FSRS wrong-answer registration (twice)    - Line 788: fetch(`/api/learning-path/interleaved-...

### `frontend/src/pages/ModernTeacherContentPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/content | Map-status: PARTIAL
- **Bulgu:** İçerik ekleme diyaloğundaki 'Dosya yüklemek için tıklayın' kutusunun hiçbir onClick/file-input'u yok — dosya yükleme tamamen dekoratif, hiçbir dosya seçilemiyor veya yüklenemiyor
- **Kanıt:** ModernTeacherContentPage.tsx:785-802: `<Box sx={{ p: 3, border: '2px dashed rgba(0, 0, 0, 0.2)', ... cursor: 'pointer', ... }} > <CloudUpload .../> <Typography variant="body2" color="text.secondary"> Dosya yüklemek için tıklayın </Typography> </Box>` — onClick yok, `<input type="file">` yok; handleCreateContent (satır 191-212) da hiçbir dosya alanı POST etmiyor.
- **Bağımsız doğrulama notu:** Independently verified against live source.  1. frontend/src/pages/ModernTeacherContentPage.tsx lines 785-802 exactly match the cited snippet: a `<Box>` styled as a dashed drop-zone with `cursor: 'pointer'` and a `CloudUpload` icon plus "Dosya yüklemek için tıklayın" text, but no `onClick`, no `<input type="file">`, no `ref`, no `FormData`, no `multipart` usage anywhere in the file (grep for `input type=|useRef|fileInputRef|handleFileChange|FormData|multipart` returned zero matches). The box is ...

### `frontend/src/pages/ModernTeacherDashboard.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/dashboard | Map-status: MOCK_ONLY
- **Bulgu:** Dashboard tamamen hardcoded veriyle çalışıyor; hiçbir API/service çağrısı yok
- **Kanıt:** ModernTeacherDashboard.tsx:41-94: `// Mock data\n  const stats = { totalStudents: 156, activeClasses: 4, pendingAssignments: 12, completedExams: 28, averageClassScore: 82.5, attendanceRate: 94.2, };` ve devamında `recentClasses`, `pendingTasks`, `upcomingLessons` sabit dizileri; dosyada fetch/apiClient/axios/useEffect import veya çağrısı yok.
- **Bağımsız doğrulama notu:** Independently verified by reading frontend/src/pages/ModernTeacherDashboard.tsx in full. Lines 41-49 contain exactly the quoted mock `stats` object (`totalStudents: 156, activeClasses: 4, pendingAssignments: 12, completedExams: 28, averageClassScore: 82.5, attendanceRate: 94.2`). Lines 78-94 contain the exact `recentClasses`, `pendingTasks`, `upcomingLessons` hardcoded arrays as quoted. Grep for `fetch|axios|apiClient|useEffect|useQuery` in this file returned zero matches, confirming no data-fet...

### `frontend/src/pages/ModernTeacherReportsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/reports | Map-status: PARTIAL
- **Bulgu:** Backend'de gerçek dosya üretimi/indirme endpoint'i yok; 'Raporu İndir' butonu sadece bir alert gösteriyor, hiçbir dosya indirilmiyor (kod içinde 'Simulate download' yorumu ile itiraf edilmiş)
- **Kanıt:** ModernTeacherReportsPage.tsx:212-219: `const handleDownloadReport = async (report: Report) => { try { // Simulate download\n      alert(`${report.baslik} raporu indiriliyor...`); } catch (error) { console.error('Rapor indirilemedi:', error); } };` — backend/app/api/teacher_classroom.py içinde /reports altında sadece GET listesi var, indirme/dosya-üretim endpoint'i yok.
- **Bağımsız doğrulama notu:** Bağımsız doğrulama tamamlandı.  1. Kod alıntısı doğru: `frontend/src/pages/ModernTeacherReportsPage.tsx:212-219` tam olarak iddia edildiği gibi — `handleDownloadReport` sadece `alert(...)` çağırıyor, hiçbir `apiClient` isteği, `fetch`, `window.open`, veya blob-download mantığı yok. "// Simulate download" yorumu da mevcut.  2. Backend tarafı grep ile bağımsız doğrulandı:    - `backend/app/api/teacher_classroom.py` router prefix `/api/v1/teacher` (satır 43). Bu dosyada SADECE `GET /reports` (satır...

### `frontend/src/pages/ModernTeacherStudentsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/students | Map-status: PARTIAL
- **Bulgu:** Backend gerçek DB'den öğrenci kaydını dönse bile ad/soyad/email/ortalama/sınav sayıları hep boş/0 dönüyor — 'gerçek' veri yolu da fiilen kullanılamaz durumda
- **Kanıt:** backend/app/api/teacher_classroom.py:196-211: `data = [ { "id": str(r.id), "student_user_id": r.student_user_id, "sinif": ..., # Extended profile fields (filled by student service integration later)\n            "ad": "", "soyad": "", "email": "", "ortalama": 0, "tamamlanan_sinav": 0, "toplam_sinav": 0, } for r in rows ]`
- **Bağımsız doğrulama notu:** Independently verified all claim components:  1. File/lines match exactly: backend/app/api/teacher_classroom.py lines 196-211 contain the quoted code verbatim (`"ad": "", "soyad": "", "email": "", "ortalama": 0, "tamamlanan_sinav": 0, "toplam_sinav": 0` with the "filled by student service integration later" comment).  2. Router is genuinely registered (not a phantom missing-endpoint): backend/routers/loader.py:114 maps `"app.api.teacher_classroom": ("teacher", "app.api.teacher_classroom")`, and ...

## P1 Bulgular (44)

### `frontend/src/components/Admin/AdminPanel.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/panel | Map-status: PARTIAL
- **Bulgu:** 'Sistem Ayarları' sekmesi (SystemSettings.tsx) tamamen sahte kaydetme — hiç API çağırmıyor
- **Kanıt:** components/Admin/SystemSettings.tsx:41-53 `const handleSave = async () => { setSaving(true); try { // Simulate API call await new Promise(resolve => setTimeout(resolve, 1000)); setSaved(true); ...`
- **Bağımsız doğrulama notu:** Independently verified by reading both frontend/src/components/Admin/SystemSettings.tsx and AdminPanel.tsx in full.  1. Quoted code matches exactly: SystemSettings.tsx lines 41-53 contain `handleSave` which does `setSaving(true)`, then `await new Promise(resolve => setTimeout(resolve, 1000))` under a comment `// Simulate API call`, then `setSaved(true)`. This is a fabricated delay with zero network I/O. 2. Grep across the entire file for `fetch(`, `axios`, `apiClient`, `api.get/post/put` returne...

### `frontend/src/components/Admin/AdminPanel.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/panel | Map-status: PARTIAL
- **Bulgu:** 'İçerik Yönetimi' sekmesinde onayla/reddet aksiyonları var olmayan path'lere gidiyor
- **Kanıt:** components/Admin/ContentManagement.tsx:275 `fetch(`/api/v1/admin/content/questions/${questionId}/approve`, ...)` ve satır 293 `.../reject` — admin.py içinde bu path'ler tanımlı değil (sadece GET/POST/PUT/DELETE /content/questions ve /content/educational/{id}/approve mevcut, ikisi de 501 döndürüyor ya da farklı kaynağa ait).
- **Bağımsız doğrulama notu:** Independently verified via Read + Grep:  1. frontend/src/components/Admin/ContentManagement.tsx lines 275 and 293 exactly match the claim:    - line 275: `fetch(`/api/v1/admin/content/questions/${questionId}/approve`, { method: 'PUT', ... })`    - line 293: `fetch(`/api/v1/admin/content/questions/${questionId}/reject`, { method: 'PUT', ... })`    And AdminPanel.tsx imports/renders `<ContentManagement />` under the tab labeled "İçerik Yönetimi" (line 22, 125, 154), so this is reachable from /admi...

### `frontend/src/components/Admin/AdminPanel.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/panel | Map-status: PARTIAL
- **Bulgu:** UserManagement sekmesinde güncelleme (PUT) formu backend'in kabul ettiği alanları göndermiyor
- **Kanıt:** components/Admin/UserManagement.tsx:148-151 `handleUpdateUser` sadece `{ad_soyad, profil_bilgileri}` gönderiyor; admin.py:158-168 PUT `/users/{kullanici_id}` sadece `is_active`/`role` alanlarını işliyor, başka alan yoksa `raise HTTPException(400, detail="Guncellenecek alan yok")` — güncelleme her zaman 400 döner.
- **Bağımsız doğrulama notu:** Independently read both files. frontend/src/components/Admin/UserManagement.tsx lines 148-151 exactly match the quoted snippet: handleUpdateUser sends {ad_soyad, profil_bilgileri} via adminService.updateUser. backend/api/admin.py lines 149-186 (kullanici_guncelle, PUT /users/{kullanici_id}) only processes is_active/role keys and raises HTTPException(400, "Guncellenecek alan yok") when neither key is present (lines 167-168), matching the cited 158-168 range closely (off-by-a-few-lines but same lo...

### `frontend/src/components/Revolutionary/RevolutionaryDashboard.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/labs | Map-status: BROKEN
- **Bulgu:** Ayar güncelleme ve sıfırlama fonksiyonları backend'e hiç gitmeden açıkça mock olarak işaretlenmiş
- **Kanıt:** revolutionaryFeaturesService.ts:762-766 `async resetRevolutionarySettings(_studentId: string) { // Mock implementation - backend API henüz hazır değil ... await new Promise(resolve => setTimeout(resolve, 600)); ... }`
- **Bağımsız doğrulama notu:** Independently verified via Read/Grep:  1. `frontend/src/services/revolutionaryFeaturesService.ts:762-786` — `resetRevolutionarySettings(_studentId)` exactly matches the quoted evidence: comment "Mock implementation - backend API henüz hazır değil", `await new Promise(resolve => setTimeout(resolve, 600))`, then returns a hardcoded `defaultSettings` object. There is no `fetch()` call at all in this function — it never touches the backend, unlike `getRevolutionarySettings` (line 677) and `updateRev...

### `frontend/src/pages/ModernAdminSettingsPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/settings | Map-status: MOCK_ONLY
- **Bulgu:** Ayarlar backend'den hiç yüklenmiyor — sabit varsayılan değerlerle başlıyor ve asla fetch edilmiyor
- **Kanıt:** ModernAdminSettingsPage.tsx:54-67 `const [settings, setSettings] = useState<SettingsState>({ siteName: 'KIRO2 Eğitim Platformu', ... })` — dosyada bu state'i güncelleyen bir GET/useEffect yok.
- **Bağımsız doğrulama notu:** Independently verified against the live file and a fresh backend grep.  1. File content matches the claim exactly: ModernAdminSettingsPage.tsx lines 54-67 initialize `settings` via `useState<SettingsState>({ siteName: 'KIRO2 Eğitim Platformu', ... })` with hardcoded defaults (siteUrl, adminEmail, maxUploadSize, sessionTimeout, toggles, logLevel). 2. No fetch/GET call exists anywhere in the file to load these settings from the backend. Only `useState` is imported from React (line 29) — `useEffect...

### `frontend/src/pages/ModernAdminUsersPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/users | Map-status: PARTIAL
- **Bulgu:** 'Düzenle' menü aksiyonu gerçek bir işlem yapmıyor, sadece alert gösteriyor
- **Kanıt:** ModernAdminUsersPage.tsx:174-177 `const handleEditUser = () => { alert(\`Kullanıcıyı düzenle: ${selectedUser?.ad_soyad}\`); handleMenuClose(); };`
- **Bağımsız doğrulama notu:** Independently verified frontend/src/pages/ModernAdminUsersPage.tsx lines 174-177: `handleEditUser` is exactly `alert(\`Kullanıcıyı düzenle: ${selectedUser?.ad_soyad}\`); handleMenuClose();` — no apiClient call, no state mutation, no navigation. Contrast with handleDeleteUser (line 179-189) and handleToggleStatus (line 191-204) in the same file, which do call apiClient.delete/patch and refetch — showing the Edit action is the outlier stub, not a misread pattern. Also confirmed the page is actuall...

### `frontend/src/pages/SystematicDebuggingPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/debug | Map-status: MOCK_ONLY
- **Bulgu:** Sayfa CLAUDE.md'nin 'debugging-first.md' protokolünü (infra-first kontrol) UI olarak taklit ediyor ama gerçek veri sağlamıyor
- **Kanıt:** Aynı dosya, INFRA_INITIAL öğeleri gerçek komutları metinsel gösteriyor (`cmd` alanı) fakat bu komutları çalıştırmıyor — kullanıcıya yanıltıcı 'her şey yeşil/PASS' izlenimi verebilir.
- **Bağımsız doğrulama notu:** Read frontend/src/pages/SystematicDebuggingPage.tsx in full and independently confirmed: (1) route /admin/debug in App.tsx:680-686 renders this exact component; (2) INFRA_INITIAL (lines 90-95) shows real commands (pg_isready -p 5434, redis-cli ping, curl /api/v1/health, curl /healthz) purely as display text in the `cmd` field; (3) grep for fetch(/axios/apiClient/api.get|post across the file returned zero matches — no network call exists anywhere in the component; (4) runInfraCheck (lines 471-489...

### `frontend/src/pages/TokenOptimizationDashboard.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/token-dashboard | Map-status: BROKEN
- **Bulgu:** CSV export endpoint'i de yok
- **Kanıt:** TokenOptimizationDashboard.tsx:115 `fetch(\`/api/v1/monitoring/export-csv?days=${timeRange}\`, ...)` — backend/api ve backend/app/api içinde `export-csv`/`export_csv` için hiçbir eşleşme bulunamadı.
- **Bağımsız doğrulama notu:** Independently verified.  1. `frontend/src/pages/TokenOptimizationDashboard.tsx:115` reads exactly as claimed:    `const response = await fetch(\`/api/v1/monitoring/export-csv?days=${timeRange}\`, { credentials: 'include' });`    inside the `exportCSV` handler.  2. Grepped `backend/api/*.py` and `backend/app/api/*.py` for `export-csv` / `export_csv` — zero route matches. The only `export_csv` hit in backend source is `backend/monitoring/token_usage_tracker.py:288`, which is a plain Python class m...

### `frontend/src/pages/YOLODetectionPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/yolo-detection | Map-status: PARTIAL
- **Bulgu:** Backend response şeması frontend'in beklediği ile uyuşmuyor — başarılı bir tespit sonrası render crash oluyor
- **Kanıt:** YOLODetectionPage.tsx: interface `DetectionResult { num_detections, detections: Detection[], processing_time }` ve `Detection { class, bbox: [number,number,number,number] }`; satır 221 `detection.bbox.map(b => b.toFixed(0))`. Ancak backend `DetectionResultResponse` (yolo_detection_api.py:100-111) `total_detections`, `processing_time_ms`, `detections: list[DetectionResponse]` ve `DetectionResponse.bbox` bir OBJE (`BoundingBoxResponse{x1,y1,x2,y2,width,height}`) — array değil. `detection.bbox.map(...)` bir objede çağrılırsa `TypeError: bbox.map is not a function` fırlatır; ayrıca `result.num_detections` ve `result.processing_time` her zaman undefined olur.
- **Bağımsız doğrulama notu:** Independently confirmed by reading both files directly (not the worktree copies, the live repo files at frontend/src/pages/YOLODetectionPage.tsx and backend/api/yolo_detection_api.py plus backend/services/yolo_question_detector.py).  Frontend TS interfaces (lines 19-30) expect: num_detections, processing_time (top-level number), Detection.class (string), Detection.bbox as a 4-tuple array.  Backend actually returns (Pydantic DetectionResultResponse lines 100-111, backed by PageDetectionResult.to_...

### `frontend/src/pages/ModernLoginPage.tsx` — Auth & Core Pages (Frontend-Backend Integration)
- Route: /login | Map-status: PARTIAL
- **Bulgu:** Login page links to /forgot-password, a route that does not exist in App.tsx — dead link falls through to the catch-all 404 redirect
- **Kanıt:** ModernLoginPage.tsx:338: `<Link to="/forgot-password" style={{ textDecoration: 'none' }}>`. `grep "forgot-password" frontend/src/App.tsx` → No matches found. Backend has the endpoint fully implemented (`backend/api/auth.py:1420 @router.post("/forgot-password"...)` and `:1475 @router.post("/reset-password"...)`, with a comment at line 1460 referencing an intended `{frontend_url}/reset-password?token=...` page), but no corresponding frontend page/route was ever built.
- **Bağımsız doğrulama notu:** Independently verified all claims: 1. `frontend/src/pages/ModernLoginPage.tsx:338` contains exactly `to="/forgot-password"` inside a `<Link>` component (confirmed via Read, lines 335-344 show it's the "Forgot Password" link below the login button). 2. `grep "forgot-password" frontend/src/App.tsx` → no matches confirmed independently. Broader search across all of `frontend/src` for `ForgotPassword`, `ResetPassword`, `forgot-password`, `reset-password` found matches only in `authService.ts` (API c...

### `frontend/src/pages/ModernRegisterPage.tsx` — Auth & Core Pages (Frontend-Backend Integration)
- Route: /register | Map-status: PARTIAL
- **Bulgu:** Telefon and Okul Kodu fields are collected in the UI but silently discarded before the API call — never sent to backend, never persisted
- **Kanıt:** ModernRegisterPage.tsx:741-763 (name="telefon") and :766-790 (name="okul_id") collect input into `formData`, but `authService.ts:36-44` builds the request payload as `{ email, ad_soyad, sifre, rol, birth_date, veli_email }` — telefon and okul_id are absent from the payload. Backend model `backend/models/user.py:30` (`KullaniciBase.telefon`) does support a telefon column but it is never populated because the frontend never forwards it; `okul_id` has no corresponding concept anywhere in `backend/api/auth.py` (grep found 0 matches) — the school-code field is pure UI decoration with no backend counterpart at all.
- **Bağımsız doğrulama notu:** Independently verified every claim:  1. ModernRegisterPage.tsx:741-763 — TextField name="telefon" bound to formData.telefon, confirmed exact lines. 2. ModernRegisterPage.tsx:766-790 — TextField name="okul_id" bound to formData.okul_id, confirmed exact lines (nested inside a conditional for ogrenci/ogretmen roles). 3. handleSubmit (line 164-187) calls `register(formData)` passing the full formData object (including telefon/okul_id) to authStore.register, which forwards to authService.register. 4....

### `frontend/src/pages/ModernRegisterPage.tsx` — Auth & Core Pages (Frontend-Backend Integration)
- Route: /register | Map-status: PARTIAL
- **Bulgu:** Client-side password validation (min 6 chars, no complexity check) is weaker than the backend's enforced policy (min 8 chars + upper+lower+digit+special char), so valid-looking passwords are rejected server-side with a generic error
- **Kanıt:** ModernRegisterPage.tsx:138-142: `if (!formData.password) {...} else if (formData.password.length < 6) { errors.password = 'Şifre en az 6 karakter olmalıdır'; }` — no uppercase/digit/special-char check. Backend `backend/models/user.py:66-100` `validate_password_strength` field_validator raises ValueError unless len>=8 AND contains uppercase AND lowercase (and further checks below for digit/special char per docstring at :82-87). A 6-7 char or all-lowercase password that passes the green client-side strength meter will be rejected by the backend with a 422, surfaced only as the generic catch-all `error.message` string in ModernRegisterPage.tsx:182-183.
- **Bağımsız doğrulama notu:** Independently verified against live files:  1. frontend/src/pages/ModernRegisterPage.tsx lines 138-142 exactly match the claim: `if (!formData.password) { errors.password = 'Şifre gerekli'; } else if (formData.password.length < 6) { errors.password = 'Şifre en az 6 karakter olmalıdır'; }` — no uppercase/lowercase/digit/special-char checks.  2. backend/models/user.py lines 66-105 (`KullaniciOlustur.validate_password_strength`, a pydantic field_validator on the `sifre` field) enforces: len>=8 (lin...

### `frontend/src/services/apiClient.ts` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: N/A | Map-status: PARTIAL
- **Bulgu:** Two parallel HTTP stacks with inconsistent 401 handling: apiClient.ts (axios) auto-refreshes the access token and retries the original request on 401 (apiClient.ts:64-81); utils/apiHelpers.ts's `apiRequest()` (used by authService for login/register/me/change-password/etc.) instead hard-redirects to /login immediately on 401 with no refresh attempt (apiHelpers.ts:449-451). A mid-session cookie expiry hitting an authService-backed call (e.g. change-password, profile update) bounces the user to /login without ever trying a silent refresh, while the identical situation on an apiClient-backed call (organizationService, examService, etc.) is silently recovered.
- **Kanıt:** apiClient.ts:64-81 (`if (error.response?.status === 401 && !originalRequest._retry) { ... await this.refreshAccessToken(); return this.client(originalRequest); }`) vs apiHelpers.ts:446-453 (`if (response.status === 401) { if (window.location.pathname !== '/login') { window.location.href = '/login'; } throw new Error('Oturum süresi doldu'); }`).
- **Bağımsız doğrulama notu:** Independently verified both files. apiClient.ts:64-81 contains the axios response interceptor that on 401 (and !originalRequest._retry) calls `await this.refreshAccessToken()` then retries the original request via `return this.client(originalRequest)` — confirmed verbatim. apiHelpers.ts exports a separate `apiRequest<T>()` function (line 432, not part of the ApiHelpers class which itself wraps apiClient) that uses raw `fetch()` with `credentials: 'include'`; on `response.status === 401` (lines 4...

### `frontend/src/services/authService.ts` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: N/A | Map-status: FULLY_WIRED
- **Bulgu:** authService uses raw `fetch()` via `apiRequest()` (utils/apiHelpers.ts) instead of the shared `apiClient` axios instance, so it does not benefit from apiClient's 401 refresh-and-retry logic (see apiClient.ts finding above); a 401 on any authService call (e.g. change-password) redirects straight to /login instead of attempting a token refresh first.
- **Kanıt:** authService.ts:2 `import { apiRequest } from '../utils/apiHelpers';` and every method (e.g. line 20, 63, 79, 123, 139) calls `apiRequest(...)` — a `fetch()` wrapper, not `apiClient`.
- **Bağımsız doğrulama notu:** Independently verified: authService.ts imports apiRequest from utils/apiHelpers (line 2) and every single method (login, register, logout, refreshToken, veliOnayVerify, veliOnayWithdraw, validateToken, getCurrentUser, updateProfile, changePassword, requestPasswordReset, resetPassword) calls apiRequest(...), never apiClient. Confirmed apiRequest (apiHelpers.ts:432-478) is a raw fetch() wrapper that on 401 immediately redirects to /login (lines 449-454) with zero refresh attempt. By contrast, apiC...

### `frontend/src/pages/AccessibilityDemoPage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /accessibility-demo | Map-status: PARTIAL
- **Bulgu:** The headline "Turkish-subtitled accessible video" demo references subtitle/caption VTT files that do not exist anywhere in the frontend static assets
- **Kanıt:** frontend/src/pages/AccessibilityDemoPage.tsx:31-47 defines `videoTracks` with `src: '/subtitles/turkish.vtt'` (id 'tr-subtitle', default:true) and `src: '/subtitles/turkish-cc.vtt'` (id 'tr-caption'), passed to `<AccessibleVideoPlayer tracks={videoTracks} .../>` at line 104. A filesystem search (`find frontend/public -iname "*.vtt"` and `find frontend -path "*/public/subtitles*"`) returned zero matches — no such files exist under frontend/public, so both subtitle/caption tracks will 404 in the browser despite the page explicitly claiming (line 81) "Türkçe altyazılı erişilebilir video player" as a completed feature.
- **Bağımsız doğrulama notu:** Independently verified all claim elements:  1. Read frontend/src/pages/AccessibilityDemoPage.tsx lines 31-47: `videoTracks` array exactly as described — `src: '/subtitles/turkish.vtt'` (id 'tr-subtitle', kind 'subtitles', default: true) and `src: '/subtitles/turkish-cc.vtt'` (id 'tr-caption', kind 'captions'). 2. Confirmed `tracks={videoTracks}` is passed to `<AccessibleVideoPlayer>` at line 104. 3. Confirmed line 81 text "Türkçe altyazılı erişilebilir video player" listed under "✅ Task 24 Tamam...

### `frontend/src/pages/ModernChatPage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /chat | Map-status: PARTIAL
- **Bulgu:** "Clear chat history" only clears local state; the backend has no DELETE /sessions route, so server-side chat rows are never removed
- **Kanıt:** frontend/src/services/chatService.ts:443-457 `clearAllSessions()` does `await fetch(`${ENHANCED_CHAT_API}/sessions`, { method: 'DELETE', ... })` (called from TurkishChatInterface.tsx:209-212 `await chatService.clearAllSessions().catch(console.error); setMessages([]);`). backend/api/enhanced_chat.py only defines `@router.post("/message")`, `@router.post("/stream")`, `@router.get("/sessions")`, `@router.get("/sessions/{session_id}/messages")`, `@router.get("/history/{student_id}")`, `@router.post("/message-with-attachment")`, `@router.post("/bionic-reading")` — no DELETE route exists, so the request 404/405s, the error is swallowed by `.catch(console.error)`, and the UI shows an empty chat while `chat_sessions`/`chat_messages` rows persist in the DB.
- **Bağımsız doğrulama notu:** Independently verified all cited evidence: 1. frontend/src/services/chatService.ts:443-457 exactly matches the claim — clearAllSessions() does `await fetch(`${ENHANCED_CHAT_API}/sessions`, { method: 'DELETE', credentials: 'include' })` wrapped in try/catch that swallows errors, then clears local state (messages, sessionId, sessionStorage) unconditionally. 2. ENHANCED_CHAT_API constant = '/api/v1/enhanced-chat' (chatService.ts:4). 3. frontend/src/components/Chat/TurkishChatInterface.tsx:209-212 c...

### `frontend/src/pages/ModernProfilePage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /profile | Map-status: PARTIAL
- **Bulgu:** Email and school-code (okul_id) fields are user-editable but silently dropped by the backend on save
- **Kanıt:** frontend/src/pages/ModernProfilePage.tsx:462-476 (email TextField, name="email", enabled when isEditing) and :493-506 (okul_id TextField) are sent via `updateProfile(formData)` -> authService.ts:160-164 `PUT ${this.baseUrl}/profile` (baseUrl='/api/v1/auth'). backend/api/auth.py:1531 `@router.put("/profile", ...)` -> :1558 `allowed_fields = ["ad", "soyad", "telefon", "profil_resmi"]` — email and okul_id are not in this list and are never persisted, yet the frontend still shows `setSuccess('Profil başarıyla güncellendi')` (ModernProfilePage.tsx:108) regardless.
- **Bağımsız doğrulama notu:** Independently verified every cited line. frontend/src/pages/ModernProfilePage.tsx:462-476 contains the email TextField (name=\"email\", disabled={!isEditing || isLoading}, i.e. editable), and :492-506 contains the okul_id TextField with identical enable logic — both exactly as claimed. handleInputChange (line 78) writes any field name into formData, and handleSave (line 96-115) calls updateProfile(formData) then unconditionally sets a success message once the promise resolves without throwing. a...

### `frontend/src/pages/ModernSettingsPage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /settings | Map-status: PARTIAL
- **Bulgu:** "Önbelleği Temizle" (Clear Cache) button is shown to all roles but the endpoint is admin-only, so it silently fails and shows a false-success message for non-admins
- **Kanıt:** frontend/src/pages/ModernSettingsPage.tsx:763-774 renders the clear-cache ModernButton outside any role gate (route itself allows `['ogrenci','ogretmen','veli','admin']`, App.tsx:702), calling :122-144 `handleClearCache` -> `fetch('/api/v1/performance/clear-cache', { method: 'POST', ... })`. backend/api/performance.py:282-285 `@router.post("/clear-cache", ...)` requires `current_user: AuthenticatedUser = Depends(get_current_admin_user)`. For a non-admin, this call fails (401/403), which is caught at ModernSettingsPage.tsx:140-143 and shown as `alert('Tarayıcı önbelleği temizlendi')` — a false success message; the real server cache is never cleared for non-admin users.
- **Bağımsız doğrulama notu:** Independently verified every claim:  1. frontend/src/pages/ModernSettingsPage.tsx:122-144 `handleClearCache` — confirmed exact code: calls `fetch('/api/v1/performance/clear-cache', { method: 'POST', ... })`, and on any failure (including non-2xx which throws via `if (!response.ok) throw`), the catch block at line 140-143 calls `alert('Tarayıcı önbelleği temizlendi')` — a false-success message regardless of actual server-side outcome.  2. frontend/src/pages/ModernSettingsPage.tsx:763-774 — confir...

### `frontend/src/pages/ModernSettingsPage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /settings | Map-status: PARTIAL
- **Bulgu:** The entire 'Genel/Bildirim/Gizlilik/Performans' settings block (13 toggles) is local component state only — no load or save API call exists anywhere in the file
- **Kanıt:** frontend/src/pages/ModernSettingsPage.tsx:59-81 `const [settings, setSettings] = useState({ darkMode, language:'tr', autoSave, emailNotifications, pushNotifications, soundNotifications, weeklyReports, profileVisibility:'private', shareProgress, analyticsOptIn, autoSync, offlineMode, dataCompression })` and :83-89 `handleSettingChange` only does `setSettings(prev => ...)`. Grep of the file shows the only network calls are `handleExportData` and `handleClearCache` (lines 91-144) — none of these 13 fields are ever sent to or loaded from a backend endpoint. "Dil" and "Profil Görünürlüğü" render as static `<Chip label="Türkçe">` (line 283) / `<Chip label="Özel">` (line 478), not backed by any settings value.
- **Bağımsız doğrulama notu:** Read frontend/src/pages/ModernSettingsPage.tsx in full. Confirmed lines 59-81 contain exactly the described useState block with all 13 fields (darkMode, language, autoSave, emailNotifications, pushNotifications, soundNotifications, weeklyReports, performanceAlerts, profileVisibility, shareProgress, analyticsOptIn, autoSync, offlineMode, dataCompression). Lines 83-89 confirm handleSettingChange only calls setSettings(prev => ...) with no side effects. The only two fetch() calls in the entire file...

### `frontend/src/pages/DailyQuestPage.tsx` — Oyunlaştırma & Sosyal (Gamification & Social)
- Route: /daily-quests (App.tsx:373) | Map-status: PARTIAL
- **Bulgu:** Görev ilerlemesini artıran backend endpoint'i (POST /{quest_id}/progress) hiçbir frontend veya backend servisi tarafından çağrılmıyor; DailyQuest.current_value gerçek oyun aksiyonlarıyla asla artmıyor.
- **Kanıt:** grep '/progress' ve 'update_quest_progress' sonucu frontend/src/**/*.tsx içinde DailyQuestPage.tsx yalnızca navigate(QUEST_ROUTE[...]) yapıyor (satır 190: 'onClick={() => navigate(QUEST_ROUTE[q.quest_type] ?? \'/dashboard\')}'), hiçbir yerde ustaCirak/cat/fsrs sayfaları da bu endpoint'i çağırmıyor. Backend tarafında da grep 'DailyQuest' --include=*.py sonucu sadece models/gamification.py, api/daily_quest_api.py ve migration dosyasında geçiyor — CAT/FSRS/duel tamamlama servislerinin hiçbiri DailyQuest.current_value'yu güncellemiyor.
- **Bağımsız doğrulama notu:** Independently verified via Read + Grep (not trusting prior report):\n1. frontend/src/pages/DailyQuestPage.tsx line 190 confirmed exactly as quoted -- the quest card's action button only does navigate(QUEST_ROUTE[...]), never calls the progress endpoint.\n2. Grepped all of frontend/src for 'daily-quests', 'DailyQuest', 'quest' -- only DailyQuestPage.tsx (calls /today and /claim-bonus), ModernStudentDashboard.tsx (fetches /today + navigates), ModernNavigation.tsx (nav link), App.tsx (route def), a...

### `frontend/src/pages/ObaSeferleriPage.tsx` — Oyunlaştırma & Sosyal (Gamification & Social)
- Route: /oba-seferleri (App.tsx:438) | Map-status: PARTIAL
- **Bulgu:** Sayfa kullanıcının gerçek oba ID'sini hiç sorgulamıyor, hardcoded 'demo-oba' string'i ile backend'i çağırıyor — bu yüzden hiçbir gerçek kullanıcı için doğru challenge/history gösterilmiyor.
- **Kanıt:** ObaSeferleriPage.tsx:26-27: '// Demo oba ID - gercek uygulamada kullanicinin obasindan gelecek\nconst DEMO_OBA_ID = \'demo-oba\';' ve satır 42-43 'obaSeferleri.getActive(DEMO_OBA_ID), obaSeferleri.getHistory(DEMO_OBA_ID, 10)'. Backend tarafında oba_seferleri_api.py:71 'ObaChallenge.oba_id == oba_id' string eşleşmesi yapıyor (models/oba_seferleri.py:40 'oba_id: Mapped[str]') — 'demo-oba' hiçbir gerçek Oba kaydına eşleşmeyecek, dolayısıyla her kullanıcı için 'Bu hafta aktif gorev yok' dönecek (satır 79-84).
- **Bağımsız doğrulama notu:** Independently verified every cited line: - frontend/src/pages/ObaSeferleriPage.tsx:26-27 literally contains `const DEMO_OBA_ID = 'demo-oba';` with the exact comment quoted. - Lines 42-43 confirm `obaSeferleri.getActive(DEMO_OBA_ID)` and `obaSeferleri.getHistory(DEMO_OBA_ID, 10)` are called on initial fetch, and line 70 shows the same hardcoded ID is reused in handleContribute's refresh call. There is no call anywhere in the file that fetches the user's real oba/team ID. - Route confirmed at fron...

### `frontend/src/pages/RealmPage.tsx` — Oyunlaştırma & Sosyal (Gamification & Social)
- Route: /realms (App.tsx:726) | Map-status: PARTIAL
- **Bulgu:** Gamification profil verisi backend'de {success, data:{...}} zarfıyla dönüyor ama frontend zarfı hiç açmadan doğrudan üst seviyeden okuyor — XP bar, seviye ve streak rozeti her zaman varsayılan (0 XP, Sv.1, streak 0) gösteriliyor.
- **Kanıt:** RealmPage.tsx:53-61: "const res = await fetch('/api/v1/gamification/profile', { credentials: 'include' }); ... const data = await res.json(); setGamification({ total_xp: data.total_xp ?? 0, current_level: data.current_level ?? 1, streak: data.streak ?? 0, streak_active_today: data.streak_active_today ?? false, });". Backend gamification_api.py:680-699: "return { \"success\": True, \"data\": { \"user_id\": user_id, \"total_xp\": total_xp, ... \"streak\": ..., \"streak_active_today\": streak_active_today, ... }, \"message\": ... }" — alanlar 'data.data.total_xp' altında, RealmPage'in okuduğu 'data.total_xp' yolunda değil, dolayısıyla her zaman undefined → ?? fallback değerleri kullanılıyor.
- **Bağımsız doğrulama notu:** Independently verified both sides of the claim against live source.  Frontend: frontend/src/pages/RealmPage.tsx lines 53-61 exactly match the quoted snippet — fetches '/api/v1/gamification/profile' and reads data.total_xp, data.current_level, data.streak, data.streak_active_today directly from the top-level parsed JSON, each with a '??' default fallback.  Backend: backend/api/gamification_api.py has router = APIRouter(prefix=\"/api/v1/gamification\", ...) (line 36) and @router.get(\"/profile\") ...

### `frontend/src/pages/ModernExamHistoryPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /exam/history (App.tsx:258) AND /exams (App.tsx:282-288, same ExamHistoryPage component confirmed by reading App.tsx:281-288) | Map-status: PARTIAL
- **Bulgu:** Even if the array/object shape were fixed, ExamSessionResponse lacks the fields the page's UI requires (subject, score, correct/wrong/empty counts, duration)
- **Kanıt:** backend/api/sinav.py:126-137 ExamSessionResponse fields: session_id, student_id, exam_type, status, total_questions, duration_minutes, current_question_index, started_at, completed_at — none of subject/correct_count/wrong_count/empty_count/score/duration used at frontend/src/pages/ModernExamHistoryPage.tsx:36-48 (interface Exam) exist on it.
- **Bağımsız doğrulama notu:** Bağımsız doğrulama: frontend/src/pages/ModernExamHistoryPage.tsx:36-48 içindeki Exam interface'i subject, correct_count, wrong_count, empty_count, score, duration alanlarını UI için zorunlu kılıyor. Bu sayfa /api/v1/osym-exam/my-exams endpoint'ini çağırıyor (satır 62-67), bu endpoint backend/api/sinav.py:264-322'de response_model=list[ExamSessionResponse] ile tanımlı. ExamSessionResponse (sinav.py:126-137) alanları gerçekten session_id, student_id, exam_type, status, total_questions, duration_mi...

### `frontend/src/pages/ModernExamHistoryPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /exam/history (App.tsx:258) AND /exams (App.tsx:282-288, same ExamHistoryPage component confirmed by reading App.tsx:281-288) | Map-status: PARTIAL
- **Bulgu:** On any fetch failure the page silently substitutes 3 hardcoded fake exam records indistinguishable from real history
- **Kanıt:** frontend/src/pages/ModernExamHistoryPage.tsx:68-110: "} catch {\n      // Mock data\n      setExams([\n        {\n          sinav_id: '1', ... score: 85, ... completed_at: '2025-11-21T10:30:00', ..."
- **Bağımsız doğrulama notu:** Read frontend/src/pages/ModernExamHistoryPage.tsx directly. Lines 60-114 exactly match the claim: fetchExamHistory() calls fetch('/api/v1/osym-exam/my-exams', {credentials:'include'}), and on !response.ok or any thrown error, the catch block (lines 68-110) sets state to 3 hardcoded fake Exam records (sinav_id '1'/'2'/'3', scores 85/75/70, dates Nov 2025, status 'completed') with no flag distinguishing them from real data — the same GlassCard list UI renders them identically to genuine history.  ...

### `frontend/src/pages/PhotoAskPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /photo-ask (App.tsx:290, lazy import App.tsx:88) | Map-status: BROKEN
- **Bulgu:** Matched-question item shape mismatch (id vs question_id, missing topic) would still break rendering even after fixing the array key name
- **Kanıt:** backend/api/photo_ask_api.py:26-37 MatchedQuestion has fields id, subject_area (no topic); frontend/src/pages/PhotoAskPage.tsx:34-42 SimilarQuestion expects question_id, subject, topic — used as React list key (line ~317-318) and rendered directly (line 326-327).
- **Bağımsız doğrulama notu:** Bağımsız doğrulama yapıldı:  1. frontend/src/pages/PhotoAskPage.tsx:34-42 — `SimilarQuestion` interface gerçekten `question_id, similarity, question_text, subject, topic, correct_answer, has_solution` alanlarını bekliyor (satırlar birebir doğrulandı).  2. backend/api/photo_ask_api.py:26-37 — `MatchedQuestion` gerçekten `id, question_text, question_image_url, exam_type, subject_area, source_book, difficulty, correct_answer, options, explanation, similarity` alanlarına sahip. `question_id` yok (`i...

### `frontend/src/pages/PhotoAskPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /photo-ask (App.tsx:290, lazy import App.tsx:88) | Map-status: BROKEN
- **Bulgu:** 'View solution' endpoint response has no solution_text/source fields the frontend renders
- **Kanıt:** backend/api/photo_ask_api.py:58-64 QuestionSolutionResponse fields: question_id, question_text, correct_answer, explanation, options — no solution_text or source. Frontend interface SolutionResult (PhotoAskPage.tsx:50-54) and render at PhotoAskPage.tsx:386-389 read solution.solution_text and solution.source, both undefined, producing a blank solution body with a static 'AI Üretim' badge.
- **Bağımsız doğrulama notu:** Bağımsız doğrulama tamamlandı.  1. Backend (`backend/api/photo_ask_api.py:58-63`): `QuestionSolutionResponse` modeli sadece `question_id`, `question_text`, `correct_answer`, `explanation`, `options` alanlarını tanımlıyor. `solution_text` veya `source` alanı YOK. `/solution/{question_id}` GET endpoint'i (satır 131-169) bu response_model ile dönüyor ve `question_bank` tablosundan `q.explanation`'ı map ediyor — `solution_text`/`source` üretmiyor.  2. Frontend (`frontend/src/pages/PhotoAskPage.tsx:5...

### `frontend/src/pages/PhotoAskPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /photo-ask (App.tsx:290, lazy import App.tsx:88) | Map-status: BROKEN
- **Bulgu:** AI-solve endpoint's field name (solution) doesn't match frontend's solution_text read, same blank-render bug
- **Kanıt:** backend/api/photo_ask_api.py:195-200 returns {question_text, solution, model, generated} — no solution_text/source keys — but frontend reads solution.solution_text at PhotoAskPage.tsx:387.
- **Bağımsız doğrulama notu:** Bağımsız doğrulama yapıldı, iddia doğru çıktı.  1. `backend/api/photo_ask_api.py:195-200` (ai_solve endpoint) tam olarak iddia edildiği gibi: ```python return {     "question_text": question_text.strip(),     "solution": result.get("solution", ""),     "model": result.get("model", ""),     "generated": result.get("generated", False), } ``` `solution_text` veya `source` anahtarı YOK — sadece `solution` var.  2. `frontend/src/pages/PhotoAskPage.tsx:50-54` `SolutionResult` interface'i `solution_tex...

### `frontend/src/pages/QuestionUploadPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /question-upload (App.tsx:690, lazy import App.tsx:71) | Map-status: BROKEN
- **Bulgu:** 'Save to bank' button is a self-admitted no-op that never calls any backend endpoint
- **Kanıt:** frontend/src/pages/QuestionUploadPage.tsx:77-89: "// Kırpılmış görsellerde henüz metin/şık yok; kalıcı kayıt question-crud + OCR/elle giriş gerektirir\n  const handleSaveToBank = async () => {\n    ...\n    alert(\n      `${croppedQuestions.length} görüntü hazır. ` +\n        'Soru metni ve şıklar tespit edilmediği için toplu kayıt bu ekranda yapılmaz; ' + ...
- **Bağımsız doğrulama notu:** Dosyayı (frontend/src/pages/QuestionUploadPage.tsx) tam okudum. Satır 77-89 iddia edilen metinle birebir eşleşiyor: handleSaveToBank fonksiyonu croppedQuestions.length kontrolü dışında sadece bir alert() gösteriyor, herhangi bir fetch/axios/apiClient çağrısı yok — gerçekten bir no-op. Route App.tsx:71 (lazy import) ve App.tsx:693'te doğrulandı, /question-upload gerçekten bu bileşeni render ediyor.  Phantom-sorun kontrolü: backend/api/question_crud_api.py içinde gerçek bir question-crud API var (...

### `frontend/src/pages/ModernParentChildrenPage.tsx` — Veli paneli (+ olası ölü kod)
- Route: /parent/children | Map-status: PARTIAL
- **Bulgu:** 4 aksiyon butonu (Detaylı Rapor, Sınav Sonuçları, Öğrenme Yolu, Öğretmenle İletişim) hiçbir onClick handler'a bağlı değil -- tamamen dekoratif, tıklanınca hiçbir şey olmuyor.
- **Kanıt:** ModernParentChildrenPage.tsx:410-424 `<ModernButton variant="glass" icon={<BarChart />} size="small">Detaylı Rapor</ModernButton>` ... 4 buton da aynı şekilde, hiçbirinde `onClick` prop yok.
- **Bağımsız doğrulama notu:** Read frontend/src/pages/ModernParentChildrenPage.tsx directly. Lines 410-424 exactly match the cited evidence: an "Action Buttons" Box containing 4 ModernButton components (BarChart/"Detaylı Rapor", Assessment/"Sınav Sonuçları", Psychology/"Öğrenme Yolu", Email/"Öğretmenle İletişim"), size="small", variant="glass" — none of them has an onClick prop. Checked ModernButton.tsx (the underlying component): it spreads ...buttonProps onto MUI's <Button>, so onClick would work if passed, but it has no d...

### `frontend/src/pages/ModernLearningPathPage.tsx` — Öğrenci Öğrenme Akışı (Student Learning Flow)
- Route: /learning-path | Map-status: PARTIAL
- **Bulgu:** Daily streak badge always shows 0/stale data because GET /api/learning-path/streak 404s and the catch block silently swallows the error.
- **Kanıt:** frontend/src/hooks/useLearningPath.ts:420-436 `apiRequest<{...}>('/api/learning-path/streak') ... catch { /* Not critical */ }` vs registered `GET /api/v1/learning-path/streak` at backend/api/learning_path_v2.py:2120. Rendered in frontend/src/pages/ModernLearningPathPage.tsx:513-520 (`streak.dailyStreak`).
- **Bağımsız doğrulama notu:** Independently verified every claim:\n1. frontend/src/hooks/useLearningPath.ts:420-436 exactly matches the citation — loadStreak() calls apiRequest('/api/learning-path/streak') and the catch block (433-435) is empty except a comment, silently swallowing errors. Initial streak state is {dailyStreak:0,...} (line 80-81).\n2. ModernLearningPathPage.tsx:513-520 renders the streak Chip only when streak.dailyStreak > 0 — confirmed verbatim.\n3. Checked backend/routers/loader.py ROUTER_MAPPING: only \"ap...

### `frontend/src/pages/ModernTeacherAssignmentsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/assignments | Map-status: PARTIAL
- **Bulgu:** API hatasında 5 kurgusal ödev sessizce state'e yazılıyor
- **Kanıt:** ModernTeacherAssignmentsPage.tsx:83-143: `} catch (error) { console.error('Ödevler yüklenemedi:', error); // Mock data\n      setAssignments([ { id: '1', baslik: 'Matematik Ödev 1 - Türev Uygulamaları', ... teslim_eden: 35, toplam_ogrenci: 45, } ...]);`
- **Bağımsız doğrulama notu:** Doğrudan dosya okuması ile doğrulandı: frontend/src/pages/ModernTeacherAssignmentsPage.tsx satır 78-147 (fetchAssignments fonksiyonu). Kod tam olarak iddia edildiği gibi:  - Satır 81: `apiClient.get('/api/v1/teacher/assignments')` gerçek bir API çağrısı yapıyor (mock değil). - Satır 83-84: `catch (error) { console.error(...) }` — hata sadece console'a loglanıyor, kullanıcıya hiçbir hata mesajı/toast gösterilmiyor. - Satır 85-143: `// Mock data` yorumu altında 5 kurgusal ödev objesi (`id: '1'` .....

### `frontend/src/pages/ModernTeacherAssignmentsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/assignments | Map-status: PARTIAL
- **Bulgu:** Backend teslim_eden ve toplam_ogrenci alanlarını her zaman 0 döndürüyor; frontend bu değerlerle 0/0 bölme yapıp 'NaN% teslim edildi' gösterebilir
- **Kanıt:** backend/app/api/teacher_classroom.py:317-330: `"teslim_eden": 0, "toplam_ogrenci": 0,` — ModernTeacherAssignmentsPage.tsx:223-225: `const getSubmissionPercentage = (teslim: number, toplam: number): number => { return Math.round((teslim / toplam) * 100); };`
- **Bağımsız doğrulama notu:** Verified both sides independently via Read (not just grep). Backend: backend/app/api/teacher_classroom.py lines 326-327 (GET /assignments list comprehension) and lines 367-368 (POST /assignments response) hardcode teslim_eden=0 and toplam_ogrenci=0 for every row — no query/join computes real submission counts. Frontend: ModernTeacherAssignmentsPage.tsx fetchAssignments() (lines 78-83) calls the real API endpoint '/api/v1/teacher/assignments' and on success sets state directly from response.data....

### `frontend/src/pages/ModernTeacherAssignmentsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/assignments | Map-status: PARTIAL
- **Bulgu:** 'Düzenle' ve 'Teslimler' aksiyonları sadece alert() gösteriyor, gerçek işlev yok
- **Kanıt:** ModernTeacherAssignmentsPage.tsx:175-195: `const handleEditAssignment = () => { alert(`Ödevi düzenle: ${selectedAssignment?.baslik}`); handleMenuClose(); };` ve `const handleViewSubmissions = () => { alert(`Teslimler: ${selectedAssignment?.baslik}`); handleMenuClose(); };`
- **Bağımsız doğrulama notu:** Independently verified in frontend/src/pages/ModernTeacherAssignmentsPage.tsx (read full file). Lines 175-178 and 192-195 exactly match the quoted code: handleEditAssignment and handleViewSubmissions both call alert() and then handleMenuClose(), with zero API interaction. These handlers are wired to both the card-level ModernButton actions (lines 517, 526) and the context-menu MenuItemComponent actions (lines 603-610), so every "Düzenle" and "Teslimler" entry point in the UI is a stub.  Checked ...

### `frontend/src/pages/ModernTeacherClassesPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/classes | Map-status: PARTIAL
- **Bulgu:** Backend her sınıf için 'ortalama_basari' alanını her zaman 0 döndürüyor — gerçek başarı hesaplaması yok
- **Kanıt:** backend/app/api/teacher_classroom.py:124-134: `data = [ { "sinif_id": str(r.id), ... "ogrenci_sayisi": student_counts.get(str(r.id), 0), "ortalama_basari": 0, } for r in rows ]`
- **Bağımsız doğrulama notu:** Bağımsız doğrulama yapıldı.  1. backend/app/api/teacher_classroom.py:124-134 satırları iddia edildiği gibi tam olarak mevcut: `list_classes()` fonksiyonu her sınıf için `"ortalama_basari": 0` sabit değerini döndürüyor (satır 131, ve create_class'ta da satır 161'de aynı sabit 0). Gerçek bir başarı ortalaması hesaplaması (örn. sınav sonuçları JOIN/AVG) yok.  2. Frontend tarafı doğrulandı: frontend/src/pages/ModernTeacherClassesPage.tsx satır 65'te `fetch('/api/v1/teacher/classes', ...)` ile TAM OL...

### `frontend/src/pages/ModernTeacherClassesPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/classes | Map-status: PARTIAL
- **Bulgu:** 'Öğrenciler', 'Sınavlar', 'Düzenle' aksiyon butonlarının hiçbirinde onClick yok — sınıf kartındaki üç aksiyon da işlevsiz
- **Kanıt:** ModernTeacherClassesPage.tsx:308-318: `<ModernButton variant="glass" icon={<People />} size="small" fullWidth> Öğrenciler </ModernButton>` (ve Sınavlar/Düzenle aynı şekilde) — hiçbirinde onClick prop yok.
- **Bağımsız doğrulama notu:** Read frontend/src/pages/ModernTeacherClassesPage.tsx directly. Lines 308-318 exactly match the cited snippet: three `ModernButton` components (Öğrenciler/People icon, Sınavlar/Assessment icon, Düzenle/Edit icon), each `variant="glass" size="small" fullWidth`, none carrying an `onClick` prop. Grep confirms no `onClick` string anywhere in that Actions block. By contrast, every other ModernButton in the same file (Tekrar Dene → loadClasses, Sınıf Oluştur → setCreateDialogOpen, FAB → setCreateDialog...

### `frontend/src/pages/ModernTeacherContentPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/content | Map-status: PARTIAL
- **Bulgu:** API hatasında 8 kurgusal içerik kaydı sessizce state'e yazılıyor
- **Kanıt:** ModernTeacherContentPage.tsx:93-185: `} catch (error) { console.error('İçerikler yüklenemedi:', error); // Mock data\n      setContents([ { id: '1', baslik: 'Türev Konusu Ders Notları', ... goruntulenme: 145, } ...]);`
- **Bağımsız doğrulama notu:** Bağımsız doğrulama:  1. Dosya/satır: frontend/src/pages/ModernTeacherContentPage.tsx satır 88-189 `fetchContents()` fonksiyonunda: `try { ... await apiClient.get('/api/v1/teacher/contents'); setContents(response?.data?.contents || []); } catch (error) { console.error(...); // Mock data\n setContents([8 hardcoded content objesi (id:1-8, Türev Konusu, Fizik Elektrik vb.)]) } finally { setLoading(false) }`. İddia edilen 93-185 aralığı ve içerik BİREBİR eşleşiyor (catch bloğu + 8 mock kayıt).  2. Ph...

### `frontend/src/pages/ModernTeacherContentPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/content | Map-status: PARTIAL
- **Bulgu:** 'Görüntüle' butonunun onClick'i yok (tamamen işlevsiz), 'İndir' butonu sadece alert() gösteriyor — hiçbir gerçek indirme/görüntüleme yok
- **Kanıt:** ModernTeacherContentPage.tsx:602-619: `<ModernButton variant="glass" icon={<Visibility />} size="small" fullWidth> Görüntüle </ModernButton>` (onClick yok) ve `<ModernButton ... icon={<Download />} ... onClick={handleDownloadContent}> İndir </ModernButton>` burada `handleDownloadContent = () => { alert(`İndir: ${selectedContent?.baslik}`); handleMenuClose(); };` (satır 240-243).
- **Bağımsız doğrulama notu:** Independently verified in frontend/src/pages/ModernTeacherContentPage.tsx: - Lines 602-610: "Görüntüle" ModernButton has icon={<Visibility />} but NO onClick prop at all — confirmed non-functional. - Lines 611-619: "İndir" ModernButton has onClick={handleDownloadContent}. - Lines 240-243: handleDownloadContent is defined as `() => { alert(\`İndir: ${selectedContent?.baslik}\`); handleMenuClose(); }` — confirmed it's just a stub alert(), no real file download (no window.open, no blob fetch, no an...

### `frontend/src/pages/ModernTeacherExamsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/exams | Map-status: PARTIAL
- **Bulgu:** API hatasında 3 kurgusal sınav (TYT Deneme Sınavı 1 vb.) sessizce state'e yazılıyor
- **Kanıt:** ModernTeacherExamsPage.tsx:85-126: `} catch (error) { console.error('Sınavlar yüklenemedi:', error); // Mock data\n      setExams([ { id: '1', baslik: 'TYT Deneme Sınavı 1', ... } ...]);`
- **Bağımsız doğrulama notu:** Doğrudan dosyayı okudum: frontend/src/pages/ModernTeacherExamsPage.tsx satır 80-130 arasında fetchExams() fonksiyonu `apiClient.get('/api/v1/teacher/exams')` çağırıyor; catch bloğunda (satır 85-126) console.error sonrası yorum "// Mock data" ile 3 kurgusal sınav (id:'1' TYT Deneme Sınavı 1 - 45 katılımcı, id:'2' AYT Matematik Sınavı, id:'3' TYT Deneme Sınavı 2 - 42 katılımcı) doğrudan setExams() ile state'e yazılıyor. Kullanıcıya bunun mock/fallback veri olduğuna dair hiçbir UI göstergesi (banne...

### `frontend/src/pages/ModernTeacherExamsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/exams | Map-status: PARTIAL
- **Bulgu:** Backend katilimci_sayisi (katılımcı sayısı) alanını her zaman 0 döndürüyor — gerçek katılım verisi yok
- **Kanıt:** backend/app/api/teacher_classroom.py:231-243: `data = [ { "id": str(r.id), ... "durum": r.durum, "olusturma_tarihi": _fmt_dt(r.created_at), "katilimci_sayisi": 0, } for r in rows ]`
- **Bağımsız doğrulama notu:** Independently verified against live source.  1. backend/app/api/teacher_classroom.py, GET /exams handler (lines 220-245): the list comprehension building the response literally hardcodes "katilimci_sayisi": 0 for every row (line 241), with no join/subquery against any exam-attempt/participation table. The POST /exams handler (create_exam, lines 248-278) also hardcodes "katilimci_sayisi": 0 in its response (line 276). Confirmed exact line content matches the cited snippet.  2. Checked for a diffe...

### `frontend/src/pages/ModernTeacherExamsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/exams | Map-status: PARTIAL
- **Bulgu:** 'Düzenle' ve 'Sonuçları Görüntüle' menü aksiyonları gerçek işlevsiz — sadece alert() gösteriyor
- **Kanıt:** ModernTeacherExamsPage.tsx:159-179: `const handleEditExam = () => { alert(`Sınavı düzenle: ${selectedExam?.baslik}`); handleMenuClose(); };` ve `const handleViewResults = () => { alert(`Sonuçları görüntüle: ${selectedExam?.baslik}`); handleMenuClose(); };`
- **Bağımsız doğrulama notu:** Doğrudan dosyayı okudum: frontend/src/pages/ModernTeacherExamsPage.tsx satır 159-162 ve 176-179 iddia edilen kodla birebir eşleşiyor: ``` const handleEditExam = () => {     alert(`Sınavı düzenle: ${selectedExam?.baslik}`);     handleMenuClose(); }; ... const handleViewResults = () => {     alert(`Sonuçları görüntüle: ${selectedExam?.baslik}`);     handleMenuClose(); }; ``` Bu iki fonksiyon sırasıyla satır 565 ve 569'daki context menu 'Düzenle' / 'Sonuçları Görüntüle' item'larına bağlı — gerçekte...

### `frontend/src/pages/ModernTeacherReportsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/reports | Map-status: PARTIAL
- **Bulgu:** API hatasında 8 kurgusal rapor (PDF/Excel/CSV, sahte boyutlarla) sessizce state'e yazılıyor
- **Kanıt:** ModernTeacherReportsPage.tsx:67-143: `} catch (error) { console.error('Raporlar yüklenemedi:', error); // Mock data\n      setReports([ { id: '1', baslik: 'Sınıf Performans Raporu - 12-A', ... boyut: '2.4 MB', } ...]);`
- **Bağımsız doğrulama notu:** Doğrudan dosyayı okudum: frontend/src/pages/ModernTeacherReportsPage.tsx satır 62-147 arasında fetchReports() fonksiyonu var. try bloğunda gerçek bir API çağrısı yapılıyor (apiClient.get('/api/v1/teacher/reports')), ancak catch bloğunda (satır 67-143) hata sadece console.error ile loglanıyor ve ardından 8 adet sabit-kodlanmış (hardcoded) kurgusal rapor state'e yazılıyor (id 1-8: "Sınıf Performans Raporu - 12-A", "Öğrenci Başarı Analizi", vb., her biri sahte "boyut" (2.4 MB, 1.8 MB, ...) ve sabit...

### `frontend/src/pages/ModernTeacherReportsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/reports | Map-status: PARTIAL
- **Bulgu:** Backend 'reports' listesi gerçek rapor dosyaları değil, sadece 3 sabit metin kartı (sınıf/öğrenci/ödev sayacı) — format='pdf'/'excel' alanı ürettiği hiçbir dosya olmadan gösteriliyor
- **Kanıt:** backend/app/api/teacher_classroom.py:519-548: `reports = [ { "id": "rpt-classes", "baslik": "Sınıf Özeti", ... "format": "pdf", "boyut": "—", }, ... ]`
- **Bağımsız doğrulama notu:** Backend teacher_classroom.py satır 519-549 (offset ±1, içerik birebir) 3 sabit rapor kartını döndürüyor; format='pdf'/'excel' alanları arkasında dosya üretimi yok, boyut='—'. Frontend ModernTeacherReportsPage.tsx gerçekten bu endpoint'i çağırıyor (mock değil) ama handleDownloadReport sadece alert() ile 'Simulate download' yapıyor, gerçek indirme/dosya üretimi yok. Ayrıca fallback mock data da tamamen statik. Grep ile backend'de /reports altında başka download/export endpoint aranmadı bulunamadı....

### `frontend/src/pages/ModernTeacherStudentsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/students | Map-status: PARTIAL
- **Bulgu:** API hatasında 5 kişilik tamamen kurgusal öğrenci listesi (Ahmet Yılmaz, Ayşe Demir vb.) sessizce state'e yazılıyor, kullanıcıya hata gösterilmiyor
- **Kanıt:** ModernTeacherStudentsPage.tsx:61-121: `} catch (error) { console.error('Öğrenciler yüklenemedi:', error); // Mock data for demo\n      setStudents([ { id: '1', ad: 'Ahmet', soyad: 'Yılmaz', email: 'ahmet.yilmaz@example.com', ... } ...]);`
- **Bağımsız doğrulama notu:** Dosyayı (ModernTeacherStudentsPage.tsx) tam okudum. Satır 56-125 arası fetchStudents fonksiyonu: satır 59'da gerçek bir API çağrısı var (`apiClient.get('/api/v1/teacher/students')`), bu iddia edilenin aksine phantom değil -- backend endpoint gerçekten mevcut (backend/app/api/teacher_classroom.py'de route tanımlı, openapi.json:49175'te de kayıtlı). Fakat catch bloğunda (satır 61-121) hata yalnızca console.error ile loglanıyor, kullanıcıya herhangi bir hata/snackbar/toast gösterilmeden 5 tamamen k...

### `frontend/src/pages/ModernTeacherStudentsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/students | Map-status: PARTIAL
- **Bulgu:** toplam_sinav her öğrenci için 0 olduğundan tüm öğrenciler yüklendiğinde 'Sınav Tamamlama' istatistiği NaN% gösterebilir (0/0 bölme)
- **Kanıt:** ModernTeacherStudentsPage.tsx:270-277: `Math.round((filteredStudents.reduce((sum, s) => sum + s.tamamlanan_sinav, 0) / filteredStudents.reduce((sum, s) => sum + s.toplam_sinav, 0)) * 100)` — backend toplam_sinav=0 döndürdüğü için (bkz. teacher_classroom.py:208) payda 0 olur.
- **Bağımsız doğrulama notu:** Bağımsız doğrulama yapıldı, iki kanıt noktası da doğru:  1. Frontend: C:\Users\husey\kiro2\frontend\src\pages\ModernTeacherStudentsPage.tsx satır 272-277 (GlassCard sarmalayıcı 270-271'den itibaren) tam olarak iddia edilen ifadeyi içeriyor: ``` Math.round(   (filteredStudents.reduce((sum, s) => sum + s.tamamlanan_sinav, 0) /     filteredStudents.reduce((sum, s) => sum + s.toplam_sinav, 0)) *     100, ) ``` Bu satır "Sınav Tamamlama" kartında gösteriliyor (satır 280).  2. Backend: C:\Users\husey\...

## P2 Bulgular (33)

### `frontend/src/pages/Admin/CuratorPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/curator | Map-status: FULLY_WIRED
- **Bulgu:** İstatistik alanı adı uyuşmuyor — 'Bugün Onaylanan' kutusu her zaman '-' gösteriyor
- **Kanıt:** useCuratorQueue.ts:89 `CuratorStats { ... verified_today: number; ... }` ve CuratorPage.tsx:689 `verifiedToday={statsQuery.data?.verified_today}` — ancak backend `StatsResponse` (curator.py:136-151) sadece `verified_count` (lifetime toplam) döndürüyor, `verified_today` alanı hiç yok.
- **Bağımsız doğrulama notu:** Independently verified against live source files (not the prior agent's report):  1. frontend/src/hooks/useCuratorQueue.ts:87-93 — `CuratorStats` interface declares `verified_today: number` (line 89) and `rejected_today: number` (line 90).  2. frontend/src/pages/Admin/CuratorPage.tsx:689 — `verifiedToday={statsQuery.data?.verified_today}` — confirmed exact match, plus line 102 shows this feeds the 'Bugün Onaylanan' stat box (`value: verifiedToday ?? '-'`), so when the field is undefined the UI f...

### `frontend/src/pages/ModernAdminDashboard.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/dashboard | Map-status: MOCK_ONLY
- **Bulgu:** Alt butonların (Kullanıcı Yönetimi / Sınav Yönetimi / İstatistikler) onClick handler'ı yok
- **Kanıt:** ModernAdminDashboard.tsx:613-620: `<ModernButton ... icon={<People />}>Kullanıcı Yönetimi</ModernButton>` — sadece 'Yenile' butonunda (622) onClick var.
- **Bağımsız doğrulama notu:** Read frontend/src/pages/ModernAdminDashboard.tsx lines 560-635 directly. Confirmed exact match to the claim: lines 613-615 (`<ModernButton ... icon={<People />}>Kullanıcı Yönetimi</ModernButton>`), 616-618 (Sınav Yönetimi), 619-621 (İstatistikler) have NO onClick prop. Only the 'Yenile' button at line 622 has `onClick={loadDashboardData}`.  Grep for onClick/useNavigate/navigate(/window.location across the whole file returned only 2 hits total (line 189 and line 622), both wired to the same `load...

### `frontend/src/pages/ModernAdminUsersPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/users | Map-status: PARTIAL
- **Bulgu:** GET başarısız olursa hardcoded mock kullanıcı listesine düşüyor (kullanıcıya sessiz yanıltıcı veri)
- **Kanıt:** ModernAdminUsersPage.tsx:94-142 catch bloğunda `// Mock data` yorumu ile 5 sabit kullanıcı set ediliyor.
- **Bağımsız doğrulama notu:** Independently verified against live files.  1. File/route: frontend/src/App.tsx:61 lazy-loads `ModernAdminUsersPage` and mounts it at path="/admin/users" (line 596) — route claim correct.  2. Mock-fallback code: In frontend/src/pages/ModernAdminUsersPage.tsx, `fetchUsers()` (lines 89-146) does call the real API — `await apiClient.get('/api/v1/admin/users')` (line 92) — so it is NOT that "no API call exists"; the previous agent read it correctly. But the `catch` block (lines 94-142) logs the erro...

### `frontend/src/pages/ModernOrgOnboardingPage.tsx` — Admin paneli (organizasyon/curator YENİ) — frontend-backend entegrasyon denetimi
- Route: /admin/organizasyon | Map-status: FULLY_WIRED
- **Bulgu:** Rol değiştirme yanıtı (PATCH) her zaman email=None döndürüyor
- **Kanıt:** org_api.py:144 `return OrgMemberOut(user_id=m["user_id"], email=None, org_role=m["org_role"])` — sayfa PATCH sonrası `fetchAll()` ile listeyi yenilediği için (ModernOrgOnboardingPage.tsx:173) kullanıcıya yansımıyor, ama API sözleşmesi tutarsız.
- **Bağımsız doğrulama notu:** Independently verified both halves of the claim with direct file reads:  1. Backend: backend/api/org_api.py line 144 reads exactly `return OrgMemberOut(user_id=m["user_id"], email=None, org_role=m["org_role"])` inside the PATCH `/members/{user_id}` handler (update_org_member, lines 122-144). This contrasts with the GET `/members` handler (lines 44-68) and POST `/members` handler (line 119), both of which populate `email` from a real JOIN (`SELECT m.user_id, u.email, m.org_role FROM org_membershi...

### `frontend/src/pages/ModernLoginPage.tsx` — Auth & Core Pages (Frontend-Backend Integration)
- Route: /login | Map-status: PARTIAL
- **Bulgu:** Post-login default-dashboard mapping for students ('/learning-path') is inconsistent with the equivalent helper in UnauthorizedPage/Modern404Page/ModernErrorPage ('/dashboard')
- **Kanıt:** ModernLoginPage.tsx:54-62 `const dashboards = { ogrenci: '/learning-path', ... }` vs UnauthorizedPage.tsx:60-73 / Modern404Page.tsx:29-42 / ModernErrorPage.tsx:38-51 all map `case 'ogrenci': return '/dashboard';`. Both target routes exist and work, so this is a UX inconsistency, not a broken flow.
- **Bağımsız doğrulama notu:** Independently verified: ModernLoginPage.tsx lines 54-62 contain `const dashboards = { ogrenci: '/learning-path', ogretmen: '/teacher/dashboard', veli: '/parent/dashboard', admin: '/admin/dashboard' }` exactly as claimed (confirmed via Read). UnauthorizedPage.tsx (lines 60-73), Modern404Page.tsx (lines 29-42), and ModernErrorPage.tsx (lines 38-51) all implement an equivalent `getDefaultDashboard` helper with `case 'ogrenci': return '/dashboard';` — confirmed via Grep+Read on all three files, matc...

### `frontend/src/pages/UnauthorizedPage.tsx` — Auth & Core Pages (Frontend-Backend Integration)
- Route: /unauthorized | Map-status: FULLY_WIRED
- **Bulgu:** Static informational page, no backend calls needed; renders real authStore `user` state (not mocked). No gaps found.
- **Kanıt:** UnauthorizedPage.tsx:56-58 `const { user } = useAuthStore();` — data comes from the real store, not a hardcoded literal.
- **Bağımsız doğrulama notu:** Verified frontend/src/pages/UnauthorizedPage.tsx directly. Line 57 exactly matches the cited evidence: `const { user } = useAuthStore();`, importing from the real Zustand store at '@/store/authStore' (line 24), not a hardcoded/mocked value. Confirmed the route is properly wired: App.tsx:21 imports UnauthorizedPage, App.tsx:222 registers it at path '/unauthorized'. Scanned the entire file for any fetch/axios/api-client usage — none found; it is a purely static informational 403 page whose only dy...

### `frontend/src/App.tsx` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: N/A (route table, lines 1-230+736) | Map-status: FULLY_WIRED
- **Bulgu:** The 43-route table matches the pre-supplied route map exactly for the ambiguous entries: /exams (App.tsx:281-288) renders ExamHistoryPage (not a distinct component); /leagues (App.tsx:461-468) renders LeaguePage (true duplicate of /league, both mapped to the same component — dead alias, not broken); /admin/panel (App.tsx:587-594) renders AdminPanel from components/Admin/AdminPanel; /admin/curator (App.tsx:659-666) renders CuratorPage from pages/Admin/CuratorPage; /admin/labs (App.tsx:668-676) renders RevolutionaryDashboard from components/Revolutionary/RevolutionaryDashboard; /admin/debug (App.tsx:678-686) renders SystematicDebuggingPage. All six previously-'???' entries are resolved, all lazy-imported and route-wired correctly; new /admin/organizasyon route (App.tsx:619-626, commit d5023fe60) is present, gated `requiredRoles={['admin']}`, and lazy-imports ModernOrgOnboardingPage (App.tsx:64).
- **Kanıt:** App.tsx:282-287 `<Route path="/exams" element={<ProtectedRoute requiredRoles={['ogrenci']}><ExamHistoryPage /></ProtectedRoute>} />`; App.tsx:461-467 `<Route path="/leagues" element={<ProtectedRoute requiredRoles={['ogrenci','admin']}><LeaguePage /></ProtectedRoute>} />` (same component as /league at line 333); App.tsx:619-626 `<Route path="/admin/organizasyon" element={<ProtectedRoute requiredRoles={['admin']}><OrgOnboardingPage /></ProtectedRoute>} />`.
- **Bağımsız doğrulama notu:** Independently verified by reading frontend/src/App.tsx directly: - Line 64: `const OrgOnboardingPage = lazy(() => import('./pages/ModernOrgOnboardingPage'));` — confirms the component variable maps to the ModernOrgOnboardingPage file, consistent with the finding's description text. - Lines 281-288: `/exams` route renders `<ExamHistoryPage />` inside ProtectedRoute requiredRoles=['ogrenci'] — exact match to claim. - Lines 329-336 and 461-468: `/league` and `/leagues` both render `<LeaguePage />` ...

### `frontend/src/components/Auth/ProtectedRoute.tsx` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: N/A (guard used by ~45 routes in App.tsx) | Map-status: FULLY_WIRED
- **Bulgu:** No separate 'RequireRole' component exists in the codebase — role/permission gating is implemented entirely inside ProtectedRoute via the `requiredRoles`/`requiredPermissions` props (reads isAuthenticated/user/loading/hasPermission/isAuthorized straight from useAuthStore). This is the sole guard mechanism; not a defect, just clarifying for anyone expecting a distinct RequireRole component per the task brief's wording.
- **Kanıt:** grep for 'RequireRole' across frontend/src returned 0 matches; ProtectedRoute.tsx:23 `const { isAuthenticated, user, loading, hasPermission, isAuthorized } = useAuthStore();` is the only role-check entry point referenced from App.tsx (e.g. App.tsx:235 `<ProtectedRoute requiredRoles={['ogrenci']}>`).
- **Bağımsız doğrulama notu:** Independently verified every cited line: ProtectedRoute.tsx:23 destructures isAuthenticated/user/loading/hasPermission/isAuthorized from useAuthStore exactly as quoted; the component implements role (requiredRoles, line 47) and permission (requiredPermissions, lines 66-69) gating inline — no separate RequireRole component wraps or delegates this logic. App.tsx:235 exactly matches the quoted usage `<ProtectedRoute requiredRoles={['ogrenci']}>`, and ProtectedRoute is used 119 times across App.tsx ...

### `frontend/src/services/apiClient.ts` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: N/A | Map-status: PARTIAL
- **Bulgu:** Base URL config, 401->refresh->retry interceptor, and cookie transport are real (not stubbed) — apiClient.ts:24-33 sets withCredentials:true, apiClient.ts:60-81 implements a genuine refresh-and-retry on 401, apiClient.ts:136-142 posts to the real backend endpoint '/api/v1/auth/refresh/secure' which exists at backend/api/auth.py:960-1012 and is registered via loader.py:34 'api.auth'. This part is FULLY_WIRED.
- **Kanıt:** apiClient.ts:136-142: `await axios.post(`${API_BASE_URL}/api/v1/auth/refresh/secure`, {}, { withCredentials: true });` matches backend/api/auth.py:960 `@router.post("/refresh/secure", ...)`.
- **Bağımsız doğrulama notu:** Independently verified every cited line.  1. frontend/src/services/apiClient.ts:24-33 — axios.create() config block includes `withCredentials: true` at line 32 (inside the object at lines 24-33). Confirmed real, not stubbed.  2. apiClient.ts:60-81 — response interceptor's error handler: checks `error.response?.status === 401 && !originalRequest._retry` (line 64), sets `_retry = true`, calls `await this.refreshAccessToken()` (line 69), then retries the original request via `this.client(originalRe...

### `frontend/src/services/apiClient.ts` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: N/A | Map-status: PARTIAL
- **Bulgu:** apiClient.isAuthenticated() and apiClient.logout() are dead code: no caller anywhere in frontend/src. The actual auth flow (authStore/authService) uses a separate raw-fetch stack (utils/apiHelpers.ts apiRequest) instead of this axios instance, so apiClient's built-in refresh-retry interceptor never engages for login/logout/session-validation calls — only for other services (organizationService, examService, etc.) that import apiClient directly.
- **Kanıt:** grep for `apiClient.isAuthenticated` and `apiClient.logout()` across frontend/src returned 0 matches (checked, no results). authStore.ts:113 calls `authService.validateToken()` and authStore.ts:250 calls `authService.logout()` — both go through `apiRequest()` in utils/apiHelpers.ts (raw `fetch`), not through `apiClient`.
- **Bağımsız doğrulama notu:** Independently verified against the live file contents: 1. frontend/src/services/apiClient.ts line 147 defines `public async logout()` and line 161 defines `public async isAuthenticated()` on the ApiClient class — exactly as claimed. 2. Grep for `apiClient.isAuthenticated` and `apiClient.logout()` across frontend/src returned zero matches — confirming these two methods are dead code with no callers. 3. frontend/src/store/authStore.ts calls `authService.validateToken()` (line 113) and `authService...

### `frontend/src/services/authService.ts` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: N/A | Map-status: FULLY_WIRED
- **Bulgu:** All 12 authService methods map 1:1 to real, DB-backed backend/api/auth.py handlers under prefix /api/v1/auth (router registered as 'api.auth' in backend/routers/loader.py:34) — no mock data, no stub. register() payload transform (ad_soyad/sifre/rol/veli_email) matches backend KullaniciOlustur schema fields exactly (backend/models/user.py:52-64).
- **Kanıt:** authService.ts:36-44 payload `{ email, ad_soyad: `${ad} ${soyad}`.trim(), sifre: password, rol, birth_date, veli_email: ... || undefined }` vs backend/models/user.py:52-64 `sifre`, `rol`, `birth_date`, `veli_email` fields on KullaniciOlustur.
- **Bağımsız doğrulama notu:** Independently verified against live source files:  1. frontend/src/services/authService.ts:36-44 — the register() payload exactly matches the claim: `{ email, ad_soyad: `${ad} ${soyad}`.trim(), sifre: password, rol, birth_date, veli_email: ... || undefined }`.  2. backend/routers/loader.py:34 confirms `"api.auth": ("auth", "api.auth")` — router is registered.  3. backend/api/auth.py:269 — `router = APIRouter(prefix="/api/v1/auth", ...)`, matching authService.ts baseUrl `/api/v1/auth`.  4. All 12...

### `frontend/src/services/organizationService.ts` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: /admin/organizasyon | Map-status: FULLY_WIRED
- **Bulgu:** All 9 organizationService methods resolve to real endpoints; both routers ('api.org_api' and 'api.org_billing_api') are registered in backend/routers/loader.py:44-47, confirmed not a 404-producing unregistered router. Consumer ModernOrgOnboardingPage.tsx fetches all 4 datasets in parallel on mount (useEffect+Promise.all, lines 101-119) with no hardcoded/mock arrays, and mutation handlers (addMember/updateMember/removeMember/signDpa) call the service and refetch — no mock/placeholder patterns found.
- **Kanıt:** backend/routers/loader.py:44 `"api.org_api": ("security", "api.org_api"),  # Faz 0 multi-tenancy` and :45-47 `"api.org_billing_api": ("security", "api.org_billing_api",)`; organizationService.ts:70 `private baseURL = '/api/v1/org';` matches org_api.py:18 `router = APIRouter(prefix="/api/v1/org", ...)` and org_billing_api.py:27 `router = APIRouter(prefix="/api/v1/org/billing", ...)`.
- **Bağımsız doğrulama notu:** Independently verified every element of the claim by reading the actual files (not trusting the prior agent's report):  1. frontend/src/services/organizationService.ts — read in full. All 9 methods (getInfo, getMembers, addMember, updateMember, removeMember, getActivation, getLicense, getDpa, signDpa) call apiClient against real paths under baseURL `/api/v1/org`.  2. backend/routers/loader.py:44-47 — grep + read confirms the exact lines quoted: `"api.org_api": ("security", "api.org_api"),  # Faz...

### `frontend/src/services/organizationService.ts` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: /admin/organizasyon | Map-status: FULLY_WIRED
- **Bulgu:** org_api.py list_org_members correctly scopes by tenant via get_current_tenant (derived server-side from the authenticated user's users.organization_id, not a client-supplied parameter) and filters m.is_active = true — matches CLAUDE.md is_active-filter requirement and prevents the IDOR/cross-tenant leak class already logged in MEMORY (get_current_membership cross-tenant authz fix). No client-facing gap.
- **Kanıt:** backend/api/org_api.py:44-65: `organization_id: str = Depends(get_current_tenant)` and SQL `WHERE m.organization_id = :org AND m.is_active = true`; core/dependencies.py:425-445 `get_current_tenant` resolves organization_id server-side from `SELECT organization_id FROM users WHERE id = :uid`.
- **Bağımsız doğrulama notu:** Independently verified against live source, not just the prior agent's report.  1. backend/api/org_api.py:44-68 (`list_org_members`) — confirmed byte-for-byte match with cited evidence:    - Line 46-47: `_membership: dict = Depends(require_org_role("SCHOOL_ADMIN", "TEACHER"))`, `organization_id: str = Depends(get_current_tenant)` — organization_id is NOT a client-supplied query/path param, it's injected server-side via FastAPI dependency.    - Line 60: SQL is `"WHERE m.organization_id = :org AND...

### `frontend/src/store/authStore.ts` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: N/A | Map-status: FULLY_WIRED
- **Bulgu:** Zustand store correctly implements httpOnly-cookie-based auth end to end: no localStorage token persistence (persist() partialize only keeps user+isAuthenticated, authStore.ts:348-351), race-condition guard on initializeAuth via module-level isInitializing/initPromise (authStore.ts:62-63, 103-151), and 2FA branch signal ('2fa_required') consistent with backend's {success:false, requires_2fa:true} contract (backend/api/auth.py:914-921 secure_login TwoFactorRequired branch). No mock data.
- **Kanıt:** authStore.ts:88-96 initial state has no `token`/`refreshToken` fields (Omit<AuthState,'token'|'refreshToken'>); authStore.ts:180-192 checks `response.requires_2fa` and returns the literal `'2fa_required'`, matching backend/api/auth.py:914-921.
- **Bağımsız doğrulama notu:** Independently verified all claimed evidence in frontend/src/store/authStore.ts: - Lines 88-96: initial state object has only isAuthenticated/user/loading/error — no token/refreshToken fields; AuthStore type is `Omit<AuthState, 'token' | 'refreshToken'>` (line 65). Confirmed. - Lines 103-151: initializeAuth uses module-level `isInitializing`/`initPromise` flags (declared lines 62-63) to guard against concurrent calls, returning the shared in-flight promise. Confirmed race-condition guard. - Lines...

### `frontend/src/store/authStore.ts` — Core Infra (apiClient/authService/store) — KIRO2 frontend
- Route: N/A | Map-status: FULLY_WIRED
- **Bulgu:** CLAUDE.md's stated 'Cookie (frontend) + Bearer (API) dual auth' is asymmetric in practice from the frontend's perspective: the frontend NEVER sends an Authorization: Bearer header anywhere in this store/service layer — it relies exclusively on httpOnly cookies (apiClient.ts:32 withCredentials:true; apiHelpers.ts:391/443 credentials:'include'). The Bearer path exists only on the backend (mevcut_kullanici_getir accepts either) for non-browser/API clients. This is consistent behavior, not a bug, but the CLAUDE.md wording could mislead a reader into expecting the frontend to use both; documenting here since the task explicitly asked to verify this claim.
- **Kanıt:** No occurrence of 'Authorization' or 'Bearer' header construction in apiClient.ts, authService.ts, or authStore.ts (all cookie-only); backend/api/auth.py:279-299 `mevcut_kullanici_getir` supports both `credentials.credentials` (Bearer) and `request.cookies.get('access_token')`, but frontend only ever populates the cookie path.
- **Bağımsız doğrulama notu:** Independently verified every cited line and claim.  1. frontend/src/services/apiClient.ts:32 — `withCredentials: true` confirmed exactly at that line, inside the axios client config; the request interceptor comment (line 47) explicitly states "No manual token handling needed - httpOnly cookies are sent automatically."  2. frontend/src/utils/apiHelpers.ts:391 and :443 — both confirmed verbatim: `credentials: 'include'` in `fetchWithErrorHandling` (line 391) and in the legacy `apiRequest` wrapper ...

### `frontend/src/pages/AccessibilityDemoPage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /accessibility-demo | Map-status: PARTIAL
- **Bulgu:** Demo video/poster use third-party placeholder assets instead of real KIRO2/EBA content
- **Kanıt:** frontend/src/pages/AccessibilityDemoPage.tsx:100-103 `src="https://www.w3schools.com/html/mov_bbb.mp4"` and `poster="https://via.placeholder.com/800x450?text=Matematik+Dersi"` — external stock/sample content, consistent with this being an internal task-completion showcase page rather than a production-integrated feature.
- **Bağımsız doğrulama notu:** Independently verified by reading frontend/src/pages/AccessibilityDemoPage.tsx lines 98-109: the AccessibleVideoPlayer component is given src="https://www.w3schools.com/html/mov_bbb.mp4" (third-party sample video host) and poster="https://via.placeholder.com/800x450?text=Matematik+Dersi" (placeholder image service), exactly as claimed. Also confirmed via Grep that this page is wired into the live router (frontend/src/App.tsx:719-722, publicly accessible route "/accessibility-demo", no auth guard...

### `frontend/src/pages/ModernProfilePage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /profile | Map-status: PARTIAL
- **Bulgu:** Notification preference switches (email/push/weekly/performance) are pure local state with no backend persistence at all
- **Kanıt:** frontend/src/pages/ModernProfilePage.tsx:59-64 `const [preferences, setPreferences] = useState({ emailNotifications: true, pushNotifications: true, weeklyReports: true, performanceAlerts: true })` and :88-94 `handlePreferenceChange` only calls `setPreferences`; no fetch/service call anywhere in the file references these values, so toggling them has zero server effect and resets on page reload.
- **Bağımsız doğrulama notu:** Read the full ModernProfilePage.tsx (690 lines). Lines 59-64 (useState preferences) and 88-94 (handlePreferenceChange only calling setPreferences) match the cited evidence exactly. Confirmed via full-file read that no fetch/axios/apiClient/service call ever touches the `preferences` object — the only other usages are the four Switch components binding to local state. Verified /profile route maps directly to this component (App.tsx:81,478) with no alternate wrapper. Searched backend/api and backe...

### `frontend/src/pages/RBACTestPage.tsx` — Diğer (chat/profile/settings/a11y)
- Route: /rbac-test | Map-status: PARTIAL
- **Bulgu:** The "RBAC Test Page" performs zero backend calls — all permission checks are a hardcoded frontend lookup table, so it cannot detect frontend/backend authorization drift
- **Kanıt:** frontend/src/pages/RBACTestPage.tsx:29-35 sources all displayed state from `useAuthStore()` (`user`, `hasPermission`) and `useRoleAccess()` (`isStudent/isTeacher/isParent/isAdmin`) with no fetch/api import anywhere in the file. `hasPermission` (frontend/src/store/authStore.ts:295-310) checks against a hardcoded `rolePermissions` object defined at authStore.ts:25-59 (e.g. `ogrenci: [{resource:'dashboard',action:'read'}, ...]`) entirely client-side — no call is ever made to verify these permissions against the actual server-enforced authorization for the `testPermissions` resources shown on the page (RBACTestPage.tsx:37-44: 'exam/create', 'students/read', 'admin/read', 'content/update', 'users/delete').
- **Bağımsız doğrulama notu:** Read the full file frontend/src/pages/RBACTestPage.tsx (326 lines). Confirmed: no fetch/api/axios import anywhere; state is sourced only from useAuthStore() (user, hasPermission) at line 29 and useRoleAccess() (isStudent/isTeacher/isParent/isAdmin) at lines 30-35, matching the cited line range. testPermissions array at lines 37-44 matches exactly (dashboard/read, exam/create, students/read, admin/read, content/update, users/delete). Verified hasPermission implementation in frontend/src/store/aut...

### `frontend/src/pages/LeaguePage.tsx` — Oyunlaştırma & Sosyal (Gamification & Social)
- Route: /league (App.tsx:330-336) ve /leagues (App.tsx:461-468) | Map-status: FULLY_WIRED
- **Bulgu:** /league ve /leagues iki ayrı route aynı LeaguePage component'ini render ediyor; /leagues hiçbir yerden linklenmiyor (dead duplicate route).
- **Kanıt:** App.tsx:330 '<Route path="/league" element={<ProtectedRoute requiredRoles={[\'ogrenci\', \'admin\']}> <LeaguePage />' ve App.tsx:461-468 aynı şekilde '<Route path="/leagues" ... <LeaguePage />'. grep sonucu: frontend/src/components/Navigation/ModernNavigation.tsx:111 ve pages/ModernStudentDashboard.tsx:216 sadece '/league' path'ini kullanıyor; '/leagues' hiçbir .tsx/.ts dosyasında referans edilmiyor (App.tsx dışında sıfır eşleşme).
- **Bağımsız doğrulama notu:** Independently verified via Read/Grep on the live files (no reliance on prior agent's claim):  1. frontend/src/App.tsx:330 defines `<Route path="/league" ... element={<ProtectedRoute requiredRoles={['ogrenci','admin']}><LeaguePage /></ProtectedRoute>} />` and App.tsx:462 defines `<Route path="/leagues" ...>` with the identical element `<LeaguePage />` wrapped in the same ProtectedRoute — both quoted line numbers and content match exactly what was claimed. 2. Grepped the entire frontend/src for th...

### `frontend/src/pages/SoruMeydaniPage.tsx` — Oyunlaştırma & Sosyal (Gamification & Social)
- Route: /soru-meydani (App.tsx:398) | Map-status: FULLY_WIRED
- **Bulgu:** socialService.soruMeydani.acceptSolution() servis fonksiyonu tanımlı ve backend'de karşılığı var, ama SoruMeydaniPage.tsx bu fonksiyonu hiçbir yerde çağırmıyor — 'Kabul Edildi' işaretleme UI'dan asla yapılamıyor.
- **Kanıt:** services/socialService.ts:182-185 'acceptSolution: (questionId, solutionId) => request(...POST .../accept/${solutionId})' tanımlı; grep 'acceptSolution' pages/SoruMeydaniPage.tsx sonucu 0 eşleşme.
- **Bağımsız doğrulama notu:** Independently verified all three claims. (1) frontend/src/services/socialService.ts lines 182-185 define `acceptSolution: (questionId, solutionId) => request(...POST .../accept/${solutionId})` exactly as claimed. (2) Read the entirety of frontend/src/pages/SoruMeydaniPage.tsx (326 lines) — grep for 'accept' (case-insensitive) only matches lines 283/293, which are read-only rendering of `s.is_accepted` (green border + a 'Kabul Edildi' success chip when already accepted). There is no `acceptSoluti...

### `frontend/src/pages/UstaCirakPage.tsx` — Oyunlaştırma & Sosyal (Gamification & Social)
- Route: /usta-cirak (App.tsx:422) | Map-status: FULLY_WIRED
- **Bulgu:** socialService.ustaCirak.submitFeedback() servis fonksiyonu tanımlı ve backend'de karşılığı var, ama UstaCirakPage.tsx hiç çağırmıyor — mentor/mentee seans değerlendirmesi UI'dan asla toplanamıyor.
- **Kanıt:** services/socialService.ts:446-450 'submitFeedback: (sessionId, data) => request(...sessions/${sessionId}/feedback...)' tanımlı; grep 'submitFeedback' pages/UstaCirakPage.tsx sonucu 0 eşleşme.
- **Bağımsız doğrulama notu:** Independently verified all three legs of the claim: 1. Frontend service function exists: frontend/src/services/socialService.ts:446-447 defines `submitFeedback: (sessionId, data) => request(...usta-cirak/sessions/${sessionId}/feedback...)`. 2. Backend counterpart is real (not phantom): backend/api/usta_cirak_api.py:278-279 defines `POST /sessions/{session_id}/feedback` -> `submit_feedback`, which persists a `MentorFeedback` row (backend/models/usta_cirak.py table `mentor_feedback`). Router is re...

### `frontend/src/pages/ExamPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /exam/:sinavId (App.tsx:266) | Map-status: PARTIAL
- **Bulgu:** Live render tree (ModernExamStart -> ModernOSYMExamInterface -> ModernExamResults branch) is fully wired to real endpoints, but the page's internal 'results' view is dead code
- **Kanıt:** frontend/src/pages/ExamPage.tsx:32 declares currentView state including 'results', and line 221 renders <ModernExamResults> when currentView==='results', but grep of the whole file shows setCurrentView('results') is never called anywhere; COMPLETED sessions instead navigate away at line 67 ("navigate(`/exam/${sessionId}/results`, { replace: true }); return;") before that state could ever be set.
- **Bağımsız doğrulama notu:** Independently verified in frontend/src/pages/ExamPage.tsx: - Line 32: `const [currentView, setCurrentView] = useState<'start' | 'exam' | 'results'>('start');` — confirmed exact text. - Line 221: `{currentView === 'results' && sessionId && (` renders `<ModernExamResults>` — confirmed exact text and line number. - Grepped the entire file for `setCurrentView` calls (lines 32, 70, 73, 75, 79, 83, 98, 112) — none of the 7 actual call sites pass `'results'`; only values used are `'exam'` and `'start'`...

### `frontend/src/pages/ExamPage.tsx` — Sınav motoru (Exam Engine) — frontend-backend entegrasyon denetimi
- Route: /exam/:sinavId (App.tsx:266) | Map-status: PARTIAL
- **Bulgu:** examService.pauseExam()/abandonExam() call backend paths that do not exist anywhere in the backend
- **Kanıt:** frontend/src/services/examService.ts:561-582 call POST /api/v1/osym-exam/{sessionId}/pause and POST /api/v1/osym-exam/{sessionId}/abandon; grep -i "pause|abandon" across backend/api/sinav.py and backend/app/api found zero matching route definitions (only the ABANDONED status enum value and a video_analytics_routes.py /pause unrelated to exams). These two methods are only invoked from examStore.ts, which itself is only imported by components/Exam/OSYMExamInterfaceRefactored.tsx — a component with no import anywhere in the codebase (dead code), so this gap has no live user impact through the routed /exam/:sinavId tree today, but is a landmine if that store is ever wired in.
- **Bağımsız doğrulama notu:** Bağımsız doğrulama tamamlandı, tüm iddialar kanıtla örtüşüyor:  1. **Frontend kodu doğrulandı**: `frontend/src/services/examService.ts:561-582` — `pauseExam()` gerçekten `POST /api/v1/osym-exam/{sessionId}/pause`, `abandonExam()` gerçekten `POST /api/v1/osym-exam/{sessionId}/abandon` çağırıyor. Satır numaraları ve içerik iddiayla birebir örtüşüyor.  2. **Backend'de route yok — phantom-sorun kontrolü yapıldı**: `backend/api/sinav.py` router prefix'i `/api/v1/osym-exam` (satır 24) — yani frontend'...

### `frontend/src/pages/ModernParentChildrenPage.tsx` — Veli paneli (+ olası ölü kod)
- Route: /parent/children | Map-status: PARTIAL
- **Bulgu:** Backend çağrısı başarısız olursa (network hatası, 401, vb.) sessizce hardcoded mock veriye düşülüyor; kullanıcıya verinin sahte olduğu belirtilmiyor.
- **Kanıt:** ModernParentChildrenPage.tsx:72-92 `catch (error: any) { console.error(...); // Mock data for demo\n setChildren([{ ogrenci_id: '1', ad_soyad: 'Ahmet Yılmaz', ... }]) }` ve satır 115-141 aynı pattern weeklyReports için.
- **Bağımsız doğrulama notu:** Read frontend/src/pages/ModernParentChildrenPage.tsx directly. The code matches the claim near-exactly: - loadChildren() catch block (lines 72-96): on ANY fetch failure (network error, non-2xx status via the `throw new Error` at line 66, JSON parse error) it does `console.error(...)` then silently calls `setChildren([...hardcoded Ahmet Yılmaz / Ayşe Yılmaz mock objects...])` (lines 75-92) with no user-visible error state, no `_setError` call (there's an unused `_error`/`_setError` pair declared ...

### `frontend/src/pages/ModernParentReportsPage.tsx` — Veli paneli (+ olası ölü kod)
- Route: /parent/reports | Map-status: BROKEN
- **Bulgu:** 'Raporu İndir' (Download) butonunun onClick handler'ı yok -- dekoratif, işlevsiz.
- **Kanıt:** ModernParentReportsPage.tsx:298-300 `<ModernButton variant="gradient" gradient={modernColors.gradients.primary} icon={<Download />}>Raporu İndir</ModernButton>` -- onClick prop yok.
- **Bağımsız doğrulama notu:** Read frontend/src/pages/ModernParentReportsPage.tsx directly. Lines 298-300 exactly match the cited code: `<ModernButton variant="gradient" gradient={modernColors.gradients.primary} icon={<Download />}>Raporu İndir</ModernButton>` with no onClick prop. Grepped the whole file for "onClick" and "handleDownload" — zero matches; "Download" only appears as the imported icon and its single usage as the `icon` prop. Read ModernButton.tsx (the wrapped component) — it merely spreads `...buttonProps` onto...

### `frontend/src/pages/ParentDashboard.tsx` — Veli paneli (+ olası ölü kod)
- Route: N/A | Map-status: DEAD_CODE
- **Bulgu:** Dosya App.tsx'te hiç import edilmiyor / hiçbir <Route>'a bağlı değil; komple ölü kod.
- **Kanıt:** grep 'ParentDashboard[^P]' frontend/src -> tek eşleşmeler ParentDashboard.tsx'in kendi içi (satır 2, 52, 289) ve App.tsx:22 yorumu ('ParentDashboard route now redirects to /parent/dashboard; no eager import needed'). App.tsx'te `import ... ParentDashboard` veya `<Route ... ParentDashboard` şeklinde hiçbir satır yok; /parent/dashboard route'u gerçekte ModernParentDashboard'a bağlı (App.tsx:53,549).
- **Bağımsız doğrulama notu:** Independently verified via Grep across frontend/src: `pages/ParentDashboard.tsx` (the file under review) is never imported anywhere in the codebase — no `lazy(() => import('./pages/ParentDashboard'))`, no static import, no re-export. The only occurrences of the string "ParentDashboard" that aren't the file's own internal content (comment, function declaration, default export) are: (1) App.tsx:22-23 comment explicitly stating the route now redirects to /parent/dashboard and no eager import is nee...

### `frontend/src/pages/ParentDashboardNew.tsx` — Veli paneli (+ olası ölü kod)
- Route: N/A | Map-status: DEAD_CODE
- **Bulgu:** Import App.tsx'te açıkça yorum satırına alınmış, hiçbir route ona işaret etmiyor.
- **Kanıt:** App.tsx:108-109 `// Veli Paneli (yeni) — kept as lazy import for future route wiring (#258 spec)\n// const ParentDashboardNew    = lazy(() => import('./pages/ParentDashboardNew'));` -- satır başında `//` ile devre dışı, dosyada başka hiçbir import/route referansı yok (grep doğrulandı).
- **Bağımsız doğrulama notu:** Read App.tsx lines 95-124: line 108-109 exactly match the claim — comment header 'Veli Paneli (yeni) — kept as lazy import for future route wiring (#258 spec)' followed by a commented-out lazy import of ParentDashboardNew. Grepped 'ParentDashboardNew' across the entire frontend/ directory (not just src) — only 2 hits: App.tsx (the commented line) and pages/ParentDashboardNew.tsx itself (its own definition/export). No route, no other import, no test file, no dynamic string reference points to it ...

### `frontend/src/pages/ParentDashboardNew.tsx` — Veli paneli (+ olası ölü kod)
- Route: N/A | Map-status: DEAD_CODE
- **Bulgu:** Wire edilse bile, çağırdığı backend router (backend/api/veli.py) açıkça DEPRECATED işaretli ve /api/v1/parent yerine kullanılması öneriliyor.
- **Kanıt:** backend/api/veli.py:28-32 `router = APIRouter(prefix="/api/v1/veli", tags=["Veli Takip Sistemi (DEPRECATED)"], deprecated=True,  # In-memory backend — use /api/v1/parent instead)`.
- **Bağımsız doğrulama notu:** Independently verified both halves of the claim by reading the actual files:  1. backend/api/veli.py lines 28-32 exactly match the quoted evidence: ``` router = APIRouter(     prefix="/api/v1/veli",     tags=["Veli Takip Sistemi (DEPRECATED)"],     deprecated=True,  # In-memory backend — use /api/v1/parent instead ) ``` The module docstring (lines 1-9) reinforces this: "tercihen /api/v1/parent (create_parent_child_relation, approve, cocuk listesi)".  2. frontend/src/pages/ParentDashboardNew.tsx ...

### `frontend/src/pages/FSRSReviewPage.tsx` — Öğrenci Öğrenme Akışı (Student Learning Flow)
- Route: /fsrs-review | Map-status: FULLY_WIRED
- **Bulgu:** Correct-answer highlighting after a user picks an option is dead code — always evaluates to undefined, so the UI never actually reveals which option was correct (explicit TODO in source).
- **Kanıt:** frontend/src/pages/FSRSReviewPage.tsx:145-147 `const _isCorrectOpt = revealed && selected && key === Object.entries(card.options!).find(() => false)?.[0]; void _isCorrectOpt; // TODO: implement correct answer highlighting`
- **Bağımsız doğrulama notu:** Read the exact quoted lines (145-147) in frontend/src/pages/FSRSReviewPage.tsx and they match verbatim: `_isCorrectOpt` is computed via `Object.entries(...).find(() => false)?.[0]`, a predicate that always returns false, so the expression always evaluates to undefined/falsy. The value is immediately discarded with `void _isCorrectOpt;` and never used to style/highlight any Button — confirmed dead code with an explicit TODO comment matching the finding. Cross-checked the backend (`backend/app/api...

### `frontend/src/pages/ModernTeacherClassesPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/classes | Map-status: PARTIAL
- **Bulgu:** Sayfa services/apiClient.ts yerine ham fetch() kullanıyor (diğer 5 öğretmen sayfasıyla tutarsız pattern, auth-refresh interceptor'larından yararlanmıyor)
- **Kanıt:** ModernTeacherClassesPage.tsx:65-71: `const response = await fetch('/api/v1/teacher/classes', { method: 'GET', headers: { 'Content-Type': 'application/json' }, credentials: 'include' });`
- **Bağımsız doğrulama notu:** Bağımsız doğrulama tamamlandı:  1. Read(ModernTeacherClassesPage.tsx) — satır 65-71 iddia edilen kodla BİREBİR eşleşiyor: `fetch('/api/v1/teacher/classes', { method: 'GET', headers: {'Content-Type': 'application/json'}, credentials: 'include' })`. Ayrıca satır 100'de POST için de aynı ham fetch pattern'i tekrarlanıyor (bulgu bunu ayrıca vurgulamamış ama aynı sorunu güçlendiriyor).  2. Grep(apiClient.ts) — auth-refresh interceptor'ların (request/response interceptors, satır 45 ve 56) gerçekten va...

### `frontend/src/pages/ModernTeacherContentPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/content | Map-status: PARTIAL
- **Bulgu:** Backend 'boyut' (dosya boyutu) alanını her zaman '—' olarak döndürüyor ve 'goruntulenme' sayacını artıran hiçbir endpoint yok — görüntülenme sayısı asla değişmez
- **Kanıt:** backend/app/api/teacher_classroom.py:409-423: `data = [ { ... "boyut": "—", "goruntulenme": r.goruntulenme, } for r in rows ]` — dosyada goruntulenme'yi artıran hiçbir PUT/PATCH endpoint yok (sadece GET/POST/DELETE var).
- **Bağımsız doğrulama notu:** Bağımsız doğrulama tamamlandı.  1. `backend/app/api/teacher_classroom.py:398-455` (`list_contents` GET, `create_content` POST) gerçekten `"boyut": "—"` sabit değerini döndürüyor (satır 418 ve 452, iddia edilenle birebir eşleşiyor).  2. Aynı dosyada `goruntulenme` alanı sadece OKUNUYOR (`r.goruntulenme`, `content.goruntulenme`) — hiçbir yerde artırılmıyor. Dosyadaki TÜM route'lar grep ile listelendi: sadece GET (`/contents`, `/classes`, `/students`, `/exams`, `/reports`), POST (`/classes`, `/exam...

### `frontend/src/pages/ModernTeacherDashboard.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/dashboard | Map-status: MOCK_ONLY
- **Bulgu:** 'Tüm Görevler' butonunun onClick handler'ı yok — tıklamanın hiçbir etkisi yok
- **Kanıt:** ModernTeacherDashboard.tsx:483-491: `<ModernButton variant="gradient" gradient={modernColors.gradients.warning} fullWidth endIcon={<ArrowForward />} sx={{ mt: 2 }}> Tüm Görevler </ModernButton>` — onClick prop yok.
- **Bağımsız doğrulama notu:** Bağımsız doğrulama yapıldı. ModernTeacherDashboard.tsx satır 483-491 alıntı birebir doğru: `<ModernButton variant="gradient" gradient={modernColors.gradients.warning} fullWidth endIcon={<ArrowForward />} sx={{ mt: 2 }}> Tüm Görevler </ModernButton>` — onClick prop tanımlı değil. Dosyada useNavigate hook'u import edilip kullanılıyor (satır 29, 39) ve diğer benzer butonlarda (satır 306, 310, 410) `onClick={() => navigate(action.path)}` / `onClick={() => navigate('/teacher/classes')}` paterni mevcu...

### `frontend/src/pages/ModernTeacherStudentsPage.tsx` — Öğretmen paneli (Teacher Dashboard)
- Route: /teacher/students | Map-status: PARTIAL
- **Bulgu:** 'Rapor' ve 'Detay' aksiyon butonlarının onClick'i yok — işlevsiz
- **Kanıt:** ModernTeacherStudentsPage.tsx:420-427: `<ModernButton variant="glass" icon={<TrendingUp />} size="small" fullWidth> Rapor </ModernButton>` ve `<ModernButton ... icon={<Assignment />} ...> Detay </ModernButton>` — onClick yok.
- **Bağımsız doğrulama notu:** Read frontend/src/pages/ModernTeacherStudentsPage.tsx lines 380-430. Lines 421-426 contain exactly the quoted JSX: ``` <ModernButton variant="glass" icon={<TrendingUp />} size="small" fullWidth>   Rapor </ModernButton> <ModernButton variant="glass" icon={<Assignment />} size="small" fullWidth>   Detay </ModernButton> ``` Neither button has an onClick prop. To rule out that a handler exists elsewhere (e.g., via a wrapper Link/navigate, or an onClick defined further down/up passed implicitly), I g...
