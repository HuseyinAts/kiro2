/**
 * Öğretmen Dashboard - Devrimsel Özellikler Entegrasyonu
 * IRT + Morfoloji Analizi, Öğrenci Profilleri
 */

import React, { useState, useEffect } from 'react';
import { useAuth } from '../hooks/useAuth';
import IRTMorphologyAnalysis from '../components/Revolutionary/IRTMorphologyAnalysis';
import { TeacherQuickActions, TeacherStats, StudentPerformanceTable } from '../components/RoleSpecific/TeacherComponents';

const TeacherDashboard: React.FC = () => {
  const { user } = useAuth();
  const [activeTab, setActiveTab] = useState<'dashboard' | 'irt-analysis' | 'student-profiles' | 'class-management'>('dashboard');

  // Örnek öğretmen istatistikleri
  const teacherStats = {
    toplam_ogrenci: 120,
    aktif_ogrenci: 95,
    ortalama_basari: 76.8,
    gelisme_trendi: 12.5
  };

  // Örnek öğrenci performans verileri
  const studentPerformanceData = [
    {
      ogrenci_id: 'student_001',
      ad_soyad: 'Ahmet Yılmaz',
      sinif: '12-A',
      son_sinav_puani: 85,
      genel_ortalama: 78.5
    },
    {
      ogrenci_id: 'student_002',
      ad_soyad: 'Ayşe Demir',
      sinif: '12-A',
      son_sinav_puani: 92,
      genel_ortalama: 88.2
    },
    {
      ogrenci_id: 'student_003',
      ad_soyad: 'Mehmet Kaya',
      sinif: '12-B',
      son_sinav_puani: 67,
      genel_ortalama: 72.1
    },
    {
      ogrenci_id: 'student_004',
      ad_soyad: 'Fatma Özkan',
      sinif: '12-B',
      son_sinav_puani: 78,
      genel_ortalama: 75.8
    },
    {
      ogrenci_id: 'student_005',
      ad_soyad: 'Ali Çelik',
      sinif: '12-C',
      son_sinav_puani: 89,
      genel_ortalama: 82.4
    }
  ];

  // Tab değiştirme
  const handleTabChange = (tab: typeof activeTab) => {
    setActiveTab(tab);
  };

  // Öğrenci detayını görüntüle
  const handleViewStudent = (studentId: string) => {
    console.log('Viewing student:', studentId);
    // Navigate to student detail page
  };

  if (!user) {
    return (
      <div className="flex items-center justify-center min-h-screen">
        <div className="text-center">
          <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600 mx-auto mb-4"></div>
          <p className="text-gray-600">Yükleniyor...</p>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gray-50">
      {/* Header */}
      <div className="bg-white shadow-sm border-b border-gray-200">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="py-6">
            <div className="flex items-center justify-between">
              <div>
                <h1 className="text-2xl font-bold text-gray-900">
                  Hoş geldiniz, {user.ad} {user.soyad} Öğretmen
                </h1>
                <p className="text-sm text-gray-600">
                  🚀 IRT + Morfoloji analizi ile desteklenen gelişmiş öğretmen paneli
                </p>
              </div>
              <div className="flex items-center space-x-4">
                <div className="text-right">
                  <div className="text-sm text-gray-600">Toplam Öğrenci</div>
                  <div className="font-medium text-blue-600">{teacherStats.toplam_ogrenci}</div>
                </div>
                <div className="text-right">
                  <div className="text-sm text-gray-600">Ortalama Başarı</div>
                  <div className="font-medium text-green-600">{teacherStats.ortalama_basari.toFixed(1)}%</div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* Tab Navigation */}
      <div className="bg-white border-b border-gray-200">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <nav className="-mb-px flex space-x-8">
            <button
              onClick={() => handleTabChange('dashboard')}
              className={`py-4 px-1 border-b-2 font-medium text-sm ${
                activeTab === 'dashboard'
                  ? 'border-blue-500 text-blue-600'
                  : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
              }`}
            >
              Ana Sayfa
            </button>
            <button
              onClick={() => handleTabChange('irt-analysis')}
              className={`py-4 px-1 border-b-2 font-medium text-sm ${
                activeTab === 'irt-analysis'
                  ? 'border-blue-500 text-blue-600'
                  : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
              }`}
            >
              🚀 IRT + Morfoloji Analizi
            </button>
            <button
              onClick={() => handleTabChange('student-profiles')}
              className={`py-4 px-1 border-b-2 font-medium text-sm ${
                activeTab === 'student-profiles'
                  ? 'border-blue-500 text-blue-600'
                  : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
              }`}
            >
              Öğrenci Profilleri
            </button>
            <button
              onClick={() => handleTabChange('class-management')}
              className={`py-4 px-1 border-b-2 font-medium text-sm ${
                activeTab === 'class-management'
                  ? 'border-blue-500 text-blue-600'
                  : 'border-transparent text-gray-500 hover:text-gray-700 hover:border-gray-300'
              }`}
            >
              Sınıf Yönetimi
            </button>
          </nav>
        </div>
      </div>

      {/* Content */}
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {/* Ana Sayfa Tab */}
        {activeTab === 'dashboard' && (
          <div className="space-y-8">
            {/* İstatistikler */}
            <div>
              <h2 className="text-lg font-semibold text-gray-900 mb-4">Genel İstatistikler</h2>
              <TeacherStats stats={teacherStats} />
            </div>

            {/* Hızlı Eylemler */}
            <div>
              <h2 className="text-lg font-semibold text-gray-900 mb-4">Hızlı Eylemler</h2>
              <TeacherQuickActions />
            </div>

            {/* Öğrenci Performans Tablosu */}
            <div>
              <h2 className="text-lg font-semibold text-gray-900 mb-4">Son Performans Durumu</h2>
              <StudentPerformanceTable 
                students={studentPerformanceData}
                onViewStudent={handleViewStudent}
              />
            </div>

            {/* Devrimsel Özellikler Özeti */}
            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
              {/* IRT Analizi Özeti */}
              <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <h3 className="text-lg font-semibold text-gray-900 mb-4">
                  🚀 IRT + Morfoloji
                </h3>
                <div className="space-y-3">
                  <div className="flex justify-between">
                    <span className="text-sm text-gray-600">Analiz Edilen Soru:</span>
                    <span className="font-medium">156</span>
                  </div>
                  <div className="flex justify-between">
                    <span className="text-sm text-gray-600">Ortalama Kalite:</span>
                    <span className="font-medium text-green-600">78.5</span>
                  </div>
                  <div className="flex justify-between">
                    <span className="text-sm text-gray-600">Yüksek Kalite:</span>
                    <span className="font-medium text-blue-600">89%</span>
                  </div>
                </div>
                <button
                  onClick={() => handleTabChange('irt-analysis')}
                  className="mt-4 w-full px-3 py-2 bg-purple-600 text-white text-sm rounded hover:bg-purple-700 transition-colors"
                >
                  Detaylı Analiz
                </button>
              </div>

              {/* Morfoloji Profilleri Özeti */}
              <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <h3 className="text-lg font-semibold text-gray-900 mb-4">
                  Öğrenci Morfoloji Profilleri
                </h3>
                <div className="space-y-3">
                  <div className="flex justify-between">
                    <span className="text-sm text-gray-600">Yüksek Yetkinlik:</span>
                    <span className="font-medium text-green-600">45 öğrenci</span>
                  </div>
                  <div className="flex justify-between">
                    <span className="text-sm text-gray-600">Orta Yetkinlik:</span>
                    <span className="font-medium text-yellow-600">38 öğrenci</span>
                  </div>
                  <div className="flex justify-between">
                    <span className="text-sm text-gray-600">Gelişim Gerekli:</span>
                    <span className="font-medium text-red-600">12 öğrenci</span>
                  </div>
                </div>
                <button
                  onClick={() => handleTabChange('student-profiles')}
                  className="mt-4 w-full px-3 py-2 bg-blue-600 text-white text-sm rounded hover:bg-blue-700 transition-colors"
                >
                  Profilleri Gör
                </button>
              </div>

              {/* ÖSYM/ETS Karşılaştırması */}
              <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <h3 className="text-lg font-semibold text-gray-900 mb-4">
                  ÖSYM/ETS Karşılaştırması
                </h3>
                <div className="text-center mb-4">
                  <div className="text-2xl font-bold text-green-600">Standartları Aşıyor</div>
                  <div className="text-sm text-gray-600">Türkçe Morfoloji Avantajı</div>
                </div>
                <div className="space-y-2 text-sm">
                  <div className="flex justify-between">
                    <span>ÖSYM Uyumluluğu:</span>
                    <span className="font-medium text-green-600">%95</span>
                  </div>
                  <div className="flex justify-between">
                    <span>ETS Standardları:</span>
                    <span className="font-medium text-green-600">%92</span>
                  </div>
                  <div className="flex justify-between">
                    <span>Morfoloji Avantajı:</span>
                    <span className="font-medium text-purple-600">%87</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        )}

        {/* IRT + Morfoloji Analizi Tab */}
        {activeTab === 'irt-analysis' && (
          <div>
            <h2 className="text-xl font-semibold text-gray-900 mb-6">
              🚀 IRT + Türkçe Morfoloji Analizi
            </h2>
            <p className="text-gray-600 mb-6">
              ÖSYM ve ETS standartlarını aşan devrimsel soru analizi sistemi. 
              Türkçe'nin zengin morfolojik yapısını IRT parametreleri ile birleştirerek 
              soru kalitesini ve öğrenci performansını daha doğru değerlendirin.
            </p>
            <IRTMorphologyAnalysis 
              teacherId={user.kullanici_id}
              classId="class_001"
            />
          </div>
        )}

        {/* Öğrenci Profilleri Tab */}
        {activeTab === 'student-profiles' && (
          <div>
            <h2 className="text-xl font-semibold text-gray-900 mb-6">
              Öğrenci Morfoloji Profilleri
            </h2>
            <p className="text-gray-600 mb-6">
              Öğrencilerinizin Türkçe morfoloji farkındalığı ve yetkinlik seviyelerini detaylı olarak inceleyin.
            </p>
            
            {/* Profil Filtreleri */}
            <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6 mb-6">
              <h3 className="text-lg font-semibold text-gray-900 mb-4">Filtreler</h3>
              <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
                <div>
                  <label className="block text-sm font-medium text-gray-700 mb-2">Sınıf</label>
                  <select className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <option value="">Tüm Sınıflar</option>
                    <option value="12-A">12-A</option>
                    <option value="12-B">12-B</option>
                    <option value="12-C">12-C</option>
                  </select>
                </div>
                <div>
                  <label className="block text-sm font-medium text-gray-700 mb-2">Yetkinlik Seviyesi</label>
                  <select className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <option value="">Tüm Seviyeler</option>
                    <option value="yuksek">Yüksek (80%+)</option>
                    <option value="orta">Orta (60-80%)</option>
                    <option value="dusuk">Düşük (60%-)</option>
                  </select>
                </div>
                <div>
                  <label className="block text-sm font-medium text-gray-700 mb-2">Morfoloji Alanı</label>
                  <select className="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <option value="">Tüm Alanlar</option>
                    <option value="suffix">Ek Tanıma</option>
                    <option value="root">Kök Belirleme</option>
                    <option value="compound">Birleşik Anlama</option>
                  </select>
                </div>
                <div className="flex items-end">
                  <button className="w-full px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors">
                    Filtrele
                  </button>
                </div>
              </div>
            </div>

            {/* Profil Listesi */}
            <div className="bg-white rounded-lg shadow-sm border border-gray-200">
              <div className="p-6">
                <h3 className="text-lg font-semibold text-gray-900 mb-4">Öğrenci Morfoloji Profilleri</h3>
                <div className="overflow-x-auto">
                  <table className="min-w-full divide-y divide-gray-200">
                    <thead className="bg-gray-50">
                      <tr>
                        <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                          Öğrenci
                        </th>
                        <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                          Genel Yetkinlik
                        </th>
                        <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                          Ek Tanıma
                        </th>
                        <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                          Kök Belirleme
                        </th>
                        <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                          Birleşik Anlama
                        </th>
                        <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                          Son Güncelleme
                        </th>
                        <th className="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                          İşlemler
                        </th>
                      </tr>
                    </thead>
                    <tbody className="bg-white divide-y divide-gray-200">
                      {studentPerformanceData.map((student, index) => (
                        <tr key={student.ogrenci_id} className="hover:bg-gray-50">
                          <td className="px-6 py-4 whitespace-nowrap">
                            <div className="flex items-center">
                              <div className="flex-shrink-0 h-8 w-8">
                                <div className="h-8 w-8 rounded-full bg-blue-500 flex items-center justify-center text-white text-sm font-medium">
                                  {student.ad_soyad.split(' ').map(n => n.charAt(0)).join('')}
                                </div>
                              </div>
                              <div className="ml-3">
                                <div className="text-sm font-medium text-gray-900">{student.ad_soyad}</div>
                                <div className="text-sm text-gray-500">{student.sinif}</div>
                              </div>
                            </div>
                          </td>
                          <td className="px-6 py-4 whitespace-nowrap">
                            <span className={`inline-flex px-2 py-1 text-xs font-semibold rounded-full ${
                              student.genel_ortalama >= 80 ? 'bg-green-100 text-green-800' :
                              student.genel_ortalama >= 60 ? 'bg-yellow-100 text-yellow-800' :
                              'bg-red-100 text-red-800'
                            }`}>
                              {student.genel_ortalama.toFixed(0)}%
                            </span>
                          </td>
                          <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                            {(Math.random() * 40 + 60).toFixed(0)}%
                          </td>
                          <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                            {(Math.random() * 40 + 60).toFixed(0)}%
                          </td>
                          <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                            {(Math.random() * 40 + 60).toFixed(0)}%
                          </td>
                          <td className="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                            {new Date().toLocaleDateString('tr-TR')}
                          </td>
                          <td className="px-6 py-4 whitespace-nowrap text-sm font-medium">
                            <button
                              onClick={() => handleViewStudent(student.ogrenci_id)}
                              className="text-blue-600 hover:text-blue-900"
                            >
                              Detay
                            </button>
                          </td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        )}

        {/* Sınıf Yönetimi Tab */}
        {activeTab === 'class-management' && (
          <div>
            <h2 className="text-xl font-semibold text-gray-900 mb-6">
              Sınıf Yönetimi
            </h2>
            <p className="text-gray-600 mb-6">
              Sınıflarınızı yönetin, öğrenci performansını takip edin ve devrimsel özelliklerle 
              desteklenen raporlar oluşturun.
            </p>
            
            <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
              <div className="text-center py-12">
                <div className="text-gray-400 mb-4">
                  <svg className="mx-auto h-12 w-12" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                  </svg>
                </div>
                <h3 className="text-lg font-medium text-gray-900 mb-2">Sınıf Yönetimi Geliştiriliyor</h3>
                <p className="text-gray-600">
                  Bu özellik yakında kullanıma sunulacak. Şu anda IRT + Morfoloji analizi 
                  ve öğrenci profilleri üzerinde çalışabilirsiniz.
                </p>
              </div>
            </div>
          </div>
        )}
      </div>
    </div>
  );
};

export default TeacherDashboard;